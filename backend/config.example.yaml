# 本地配置模板。生产环境建议使用 OTS_DATABASE_URL 环境变量覆盖。
database:
  url: "mysql+pymysql://root:your-password@localhost:3306/ots_test?charset=utf8mb4"

# 生产环境必须通过环境变量设置 OTS_AUTH_SECRET（至少 32 个字符），不得提交真实值。
# OTS_ALLOWED_ORIGIN=http://localhost:5173
# OTS_COOKIE_SECURE=false
