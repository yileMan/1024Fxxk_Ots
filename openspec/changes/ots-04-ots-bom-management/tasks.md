## 1. 测试与验收契约准备

- [x] 1.1 为 OTS 管理员权限、字段规范化、同名同版本唯一、乐观锁、共享引用保留和禁止停用/删除编写后端失败测试。
- [x] 1.2 为产品版本 OTS 关联的查询、唯一性、创建人、无历史移除、有历史拒绝及同事务审计编写后端和 MySQL 集成失败测试。
- [x] 1.3 为 CSV 固定表头、文件与逐行校验、新建/复用/幂等/字段冲突、整批原子性和错误定位编写后端失败测试。
- [x] 1.4 为 OTS 一级导航、工作台卡片、OTS 列表/表单、关联产品查看、清单维护、CSV 错误展示及三步建档向导失败恢复编写前端组件测试。
- [x] 1.5 为管理员三步建档、OTS 维护、清单导入导出和非管理员拒绝访问编写 Playwright 冒烟测试。

## 2. 数据库、API 与 CSV 契约

- [ ] 2.1 新增下一编号 SQL 迁移及 rollback 文档，严格按 11 表基线创建 `ots_component`、`product_ots` 的字段、外键、唯一键和索引，不增加状态、软删除、暂存或历史表。
- [ ] 2.2 验证迁移可在空库和 OTS-03 升级库执行，验证回滚顺序，并检查当前阶段应用基础表名称与数量符合数据基线。
- [ ] 2.3 定义 OTS、关联产品版本、产品 OTS 清单和结构化 CSV 错误的 API Schema，固定 CSV 表头为 `ots_name,ots_version,official_website,is_eol`。
- [ ] 2.4 定义 OTS、产品版本 OTS 关联、模板、原子导入和导出端点及稳定错误码，并更新 OpenAPI 契约。
- [ ] 2.5 重新生成前端 TypeScript 类型和客户端，执行重复生成一致性检查并移除本变更造成的手写重复模型。

## 3. 数据与后端实现

- [ ] 3.1 实现 `ots_component`、`product_ots` SQLAlchemy 模型和仓储，支持 OTS 分页筛选、详情、关联产品版本及产品版本清单查询。
- [ ] 3.2 实现 OTS 创建和带 `row_version` 的编辑服务，统一去除名称/版本首尾空格，以数据库唯一键兜底并禁止停用或删除。
- [ ] 3.3 实现关联添加与受保护移除服务，保证同一产品版本/OTS 不重复、记录 `created_by`、不删除 OTS 主数据并拒绝移除具有下游历史的关联。
- [ ] 3.4 实现 UTF-8 CSV 模板和只读导出，使用固定表头与确定性行顺序，确认下载和导出不写 `audit_log`。
- [ ] 3.5 实现 CSV 两阶段原子导入：先汇总文件、表头、字段、重复和主数据冲突错误，再在单一事务中创建缺失 OTS、复用一致 OTS 并创建缺失关联。
- [ ] 3.6 实现单条写入逐对象审计和 CSV 导入单条 `batch_upsert` 汇总审计，保证权限拒绝、校验失败、冲突及事务回滚不产生审计。
- [ ] 3.7 接入 OTS 与产品清单 FastAPI 路由，复用管理员依赖并映射未找到、唯一冲突、历史引用冲突、乐观锁和结构化 CSV 错误。

## 4. 前端实现

- [ ] 4.1 新增仅管理员可见的 OTS 一级导航、路由和管理页面，更新工作台四张模块卡片，并实现按名称、版本、是否 EOL 的分页筛选及加载、空和服务错误状态。
- [ ] 4.2 实现 OTS 创建/编辑表单、共享关联产品版本展示及并发冲突处理；不展示停用或删除入口，并在冲突时保留未保存输入。
- [ ] 4.3 实现可复用的产品版本 OTS 清单组件，支持选择已有 OTS、查看当前清单、确认移除及历史引用冲突反馈。
- [ ] 4.4 实现 CSV 模板下载、文件选择、导入进度、逐行逐字段错误列表、修正后重试和当前清单导出。
- [ ] 4.5 将产品建档向导扩展为三步，在第 3 步复用清单组件；失败时保留已创建产品、版本、选择和文件错误上下文，并支持稍后继续。

## 5. 集成、文档与验收

- [ ] 5.1 运行后端 pytest 和 MySQL 集成测试，验证唯一键竞态、乐观锁、关联历史保护、CSV 原子性、幂等和审计事务一致性。
- [ ] 5.2 运行前端 Vitest、类型检查、生产构建与 Playwright，验证 OTS 管理、三步建档、清单导入导出、错误恢复和权限拒绝旅程。
- [ ] 5.3 检查本变更新增代码覆盖率不低于 80%，执行 OpenAPI 漂移及重复生成一致性检查。
- [ ] 5.4 核对数据库仍严格遵循 11 张应用基础表设计，确认 `ots_component` 无状态/人员字段、`product_ots` 不复制 OTS 业务字段且无额外业务表。
- [ ] 5.5 更新相关 API/CSV 使用说明与需求追溯，记录 FR-MASTER-001 的基线取舍并核对 FR-MASTER-001、FR-MASTER-004、FR-MASTER-005、FR-MASTER-006。
- [ ] 5.6 执行 `openspec validate ots-04-ots-bom-management --strict`，确认 proposal、delta specs、design、tasks 和全部 Given/When/Then 场景通过严格校验。
