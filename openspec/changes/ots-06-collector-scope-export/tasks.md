## 1. 契约与失败测试

- [x] 1.1 固定 `GET /api/v1/collector-scope` 的预览响应、`GET /api/v1/collector-scope/export` 的 CSV/响应头、`401/403/500` 错误码和 UTC 时间格式，并确认字段不包含产品内部信息
- [x] 1.2 先新增后端失败测试，覆盖启用产品/版本筛选、跨产品按 OTS ID 去重、空范围、实时关联变化及 SQL 层稳定排序
- [x] 1.3 先新增覆盖历史失败测试，覆盖多批次最近成功时间、后续 `failed/not_run` 不推进、首次采集、无比较基线和损坏成功批次 JSON
- [x] 1.4 先新增 CSV 字节契约失败测试，覆盖固定表头、UUID v4、UTF-8 无 BOM、CRLF、转义、空字段、OTS ID 顺序、唯一导出 ID 和实际字节 SHA-256
- [x] 1.5 先新增权限与只读失败测试，覆盖未登录 `401`、非管理员 `403`、管理员成功，以及预览、下载和失败请求前后 `audit_log`、`import_batch` 与业务数据无变化
- [x] 1.6 先新增前端页面/API 失败测试，覆盖加载、刷新、空范围、首次采集、无基线、范围增减、下载成功、下载失败和 403 页面

## 2. 数据迁移与模型

- [ ] 2.1 新增下一编号 MySQL 迁移，按 11 表数据基线完整创建 `import_batch` 全部字段、JSON 列、状态时间字段、唯一键、索引及 `imported_by` 外键，不增加其他业务表
- [ ] 2.2 新增对应回滚说明，明确先回滚应用引用、仅空表且无下游外键时删表、已有批次必须备份并禁止自动级联删除
- [ ] 2.3 新增完整 SQLAlchemy `ImportBatch` 模型并接入模型元数据，保持字段可空性、长度、JSON 类型、索引与迁移一致
- [ ] 2.4 在真实 MySQL 上验证空库和 001～008 升级库迁移、重复执行检测、JSON 写读、唯一键、外键、索引、应用回滚前置检查和受控回滚

## 3. 后端采集范围能力

- [ ] 3.1 实现 Repository 的有效范围 SQL 查询，按启用产品和启用产品版本筛选、数据库内按 OTS ID 去重并稳定升序，不读取无关内部字段
- [ ] 3.2 实现 Repository 的成功批次索引分页查询，以 `finished_at`、`created_at`、`id` 降序提供 `manifest_json` 与 `scope_coverage_json`
- [ ] 3.3 实现 Service 的历史 JSON 结构校验、最近有效范围快照选择、当前/历史集合差异和无比较基线语义
- [ ] 3.4 实现 Service 的逐 OTS 最近 `succeeded` 覆盖时间选择，确保 `failed/not_run` 不推进、首次采集返回空值且时间统一为 UTC RFC 3339 毫秒格式
- [ ] 3.5 实现不可变请求级范围快照及只读一致事务，确保预览/导出不提交、不审计，损坏历史返回 `COLLECTOR_SCOPE_HISTORY_INVALID` 且不泄露 JSON 内容
- [ ] 3.6 实现规范 CSV 字节生成、标准字段转义、UUID v4 `scope_export_id`、固定文件名和对完整响应字节计算的 SHA-256
- [ ] 3.7 新增预览/下载 Schema 与路由，复用 `require_admin`，注册服务和 router，并为下载返回 `X-Scope-Export-ID`、`X-Content-SHA256` 与 `Content-Disposition`
- [ ] 3.8 运行并修复后端单元、API 和 MySQL 集成测试，确认空范围下载只有表头、生成失败无部分文件、当前范围和历史读取符合设计

## 4. 前端采集范围页面

- [ ] 4.1 更新 OpenAPI 文档并重新生成 `front/src/api/generated.ts`，验证重复生成无漂移，前端不手写重复的预览数据模型
- [ ] 4.2 实现 collector-scope API client，预览响应使用生成类型，下载使用 Blob 并读取文件名、导出 ID 和 SHA-256 响应头
- [ ] 4.3 新增 `/system/data-exchange/collector-scope` 管理员路由和“数据交换－采集范围”导航入口，直接路由越权继续进入明确 403 页面
- [ ] 4.4 实现范围数量、比较基线、增减摘要和 OTS 明细展示，官网使用安全链接，覆盖时间为空时明确显示“首次采集”
- [ ] 4.5 实现首次加载、手动刷新、空范围、无比较基线、加载失败和重试状态，不把旧数据显示为最新结果
- [ ] 4.6 实现管理员 CSV 下载、进行中防重复、成功摘要提示和下载后刷新；下载失败不生成本地空文件且显示可重试反馈
- [ ] 4.7 运行并修复 Vitest、组件测试、类型检查和生产构建，验证文件内容、导出 ID 或摘要不写入浏览器持久存储

## 5. 集成与纵向验收

- [ ] 5.1 验证“同一 OTS 关联多个启用产品版本 → 页面和 CSV 均只有一行 → 摘要匹配下载字节”的管理员纵向场景
- [ ] 5.2 验证“新增有效关联 → 显示新增；停用/移除最后有效关联 → 显示移除且下一次 CSV 不再包含；历史 OTS、批次和审计不删除”的范围变化场景
- [ ] 5.3 验证“较早成功覆盖 → 较新失败或未运行 → 页面和 CSV 保留较早覆盖时间；从未成功 OTS 显示首次采集”的逐 OTS 场景
- [ ] 5.4 验证管理员可预览/下载、普通用户页面与直接 API 均被拒绝、未登录被拒绝，并检查响应不包含产品、人员、授权、漏洞或评估字段
- [ ] 5.5 使用系统 Chrome 运行 Playwright 采集范围冒烟场景；若 Chrome 不可用则检查系统 Edge，不下载 Playwright Chromium并保留真实环境错误
- [ ] 5.6 在代表性 200 个 OTS 和多批次数据上记录查询次数与响应时间，确认批次分页和提前结束生效且正常规模请求满足 3 秒目标
- [ ] 5.7 运行完整后端 pytest、前端 Vitest、Playwright、类型检查、生产构建和 OpenAPI 漂移检查，修复本 change 引入的回归
- [ ] 5.8 检查前后端新增代码覆盖率均不低于 80%，范围筛选、历史 JSON、CSV 字节、权限、错误和只读边界场景达到要求

## 6. 文档与最终验收

- [ ] 6.1 更新后端与前端开发文档，说明预览/下载 API、CSV 列与字节规则、摘要验证、管理员权限、错误处理、迁移和受控回滚，不记录真实凭据
- [ ] 6.2 更新 `doc/Task.md`：将 `import_batch` 完整建表归属调整到 OTS-06，并将 OTS-07 改为复用既有表实现包契约与写入，保持依赖顺序和 11 表基线不变
- [ ] 6.3 对照 `FR-EXCH-001`、`FR-EXCH-002`、`FR-EXCH-016`、`FR-EXCH-017` 更新需求追溯与测试证据，并确认外网采集、数据包导入和覆盖推进未被引入
- [ ] 6.4 执行 `openspec validate ots-06-collector-scope-export --strict --no-interactive` 并修复全部校验错误
- [ ] 6.5 复核数据库仍不超过 11 张应用基础表、OpenAPI 与 TypeScript 类型一致、采集范围操作零业务写入且零审计、迁移与回滚证据完整
