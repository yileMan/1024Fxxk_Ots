## 1. 测试基线与 API 契约

- [ ] 1.1 为评估详情先编写失败的后端测试，覆盖当前/历史修订、有效产品范围、管理员与审核人只读、负责人可编辑、退回/复评原因、候选分区及 `403/404` 不泄露语义。
- [ ] 1.2 为草稿更新先编写失败的 service/API 测试，覆盖完整核心字段、部分草稿、空白归一化、适用性依据、高风险处置说明、非法枚举、文本上限、稳定字段路径和保存不改变状态/修订号。
- [ ] 1.3 为权限、幂等和并发先编写失败的测试，覆盖非负责人、范围撤销、已提交/已完成/非当前修订、匹配版本、旧 `row_version`、无变化重复保存以及错误时无部分更新。
- [ ] 1.4 定义 `GET /api/v1/assessments/{assessment_id}` 与 `PUT /api/v1/assessments/{assessment_id}/draft` 的 Pydantic 请求/响应模型、处置枚举和 `403/404/409/422` 稳定错误契约，并用 OpenAPI 测试锁定字段与示例。

## 2. 数据查询、更新与审计

- [ ] 2.1 实现评估详情 repository 查询，关联产品、版本、产品 OTS、OTS、漏洞和唯一候选匹配，并复用有效产品范围以区分不存在与越权资源。
- [ ] 2.2 实现草稿规范化和跨字段校验，允许未完成草稿保存，同时要求非 `pending` 适用性具有 `applicability_basis`，且 `accept_risk`/`no_action` 具有 `treatment_detail`。
- [ ] 2.3 实现保存前的当前负责人、有效产品范围、当前修订和可编辑状态复核，并确保管理员、审核人及其他有范围用户不能代写。
- [ ] 2.4 实现包含评估 ID、当前修订、可编辑状态、负责人和 `row_version` 的原子条件更新；实际变化递增版本和更新时间，无变化保存保持版本、时间和数据库内容不变。
- [ ] 2.5 在实际草稿变化事务中追加 `product_assessment` 更新审计，记录修订、变更字段、枚举前后值及长文本长度/哈希摘要，确认不复制完整证据正文且失败回滚不留审计。
- [ ] 2.6 使用 MySQL 两个独立 session 验证并发更新只有一个成功、失败方不覆盖数据；验证业务更新与审计同事务、11 张应用基础表和现有索引均不变化。

## 3. 后端服务与路由

- [ ] 3.1 实现评估详情 service，返回统一详情 DTO、服务端 `editable` 判定、原因提示和 OTS-11 来源事实/候选语义，且不返回其他产品草稿或 CVSS v4.0 字段。
- [ ] 3.2 实现草稿保存 service，对零行更新区分 `ASSESSMENT_NOT_EDITABLE` 与 `ASSESSMENT_VERSION_CONFLICT`，并确保错误详情不回显未授权草稿。
- [ ] 3.3 新增评估详情和草稿路由并注册 service/router，接入现有 Cookie 身份恢复和全局错误响应，记录不含草稿正文、证据、Cookie 的结构化日志。
- [ ] 3.4 运行后端专项与完整 pytest、覆盖率及 MySQL 集成测试，确保新增代码覆盖率不低于 80%，OTS-10 任务生成和 OTS-11 待办/漏洞查询无回归。

## 4. OpenAPI 与前端测试

- [ ] 4.1 从后端重新生成 `front/src/api/openapi.json` 和 `front/src/api/generated.ts`，实现评估详情/保存薄 API client，并验证连续两次生成无漂移且不手写重复 DTO。
- [ ] 4.2 为评估详情页先编写失败的 Vitest/组件测试，覆盖加载、来源/候选/当前结论分区、退回/复评原因、明确保存、字段错误聚焦、服务失败、只读和保存成功反馈。
- [ ] 4.3 编写并发与状态变化组件测试，验证 `409` 时保留本地输入、不自动重放或覆盖，版本冲突提供刷新动作，不可编辑冲突禁用后续保存。
- [ ] 4.4 扩展路由和入口测试，验证待办项可进入 `/system/assessments/:assessmentId`，漏洞详情仅对明确可见评估提供入口，未登录仍保留目标地址。

## 5. 前端评估编辑页

- [ ] 5.1 新增单页评估详情路由与页面，按顶部原因、来源事实、候选匹配、当前产品结论顺序展示，并将所有外部文本和证据作为纯文本渲染。
- [ ] 5.2 实现分析摘要、触发条件、涉及功能/接口、适用性及依据、产品影响、现有控制、处置方式及说明、证据说明的完整表单和条件提示，不提前加入环境评分、提交或审核控件。
- [ ] 5.3 实现明确保存状态机：保存时防重复请求，成功后以服务端响应重置表单和 `row_version`，`422` 映射字段，其他失败保留输入并允许重试。
- [ ] 5.4 实现只读与并发恢复体验：按服务端 `editable` 控制表单，在版本冲突时保留未保存内容并提示确认刷新，在状态变化后停止编辑且不误报成功。
- [ ] 5.5 更新 OTS-11 待办及允许的漏洞详情入口，保持筛选返回路径，并确认管理员、审核人和其他非负责人页面不出现可用保存操作。

## 6. 集成、文档与验收

- [ ] 6.1 增加 Playwright 纵向流程：负责人从待办进入并保存部分草稿；`not_affected`/`accept_risk`/`no_action` 缺依据被拒绝；非负责人只读；两个客户端旧版本保存产生冲突且不覆盖，并使用系统 Chrome 运行。
- [ ] 6.2 运行前端 Vitest 覆盖率、`npm run typecheck`、`npm run build` 和 API 漂移检查，确保新增代码覆盖率不低于 80% 且现有工作台、待办和漏洞详情无回归。
- [ ] 6.3 检查 FR-ASSESS-001、003、004、006～010、012 的 OTS-12 草稿编辑追溯，明确 FR-ASSESS-009 的提交转换及提交人留痕仍由 OTS-14 完成，CVSS 环境评分仍由 OTS-13 完成。
- [ ] 6.4 更新前后端使用说明和 `doc/Task.md` 的 OTS-12 状态/完成证据，记录 API、字段枚举、权限、并发、审计摘要、测试结果和后续 change 边界。
- [ ] 6.5 运行 `openspec validate ots-12-assessment-editor-core --strict --no-interactive` 及全量 OpenSpec 严格校验，确认 proposal/spec/design/tasks、需求追溯、11 表约束和无迁移结论一致。
