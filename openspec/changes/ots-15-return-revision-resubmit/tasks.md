## 1. 测试基线与契约

- [ ] 1.1 先为审核退回创建下一修订编写后端失败测试，覆盖业务快照继承、事件字段清空、父子链、唯一 current、连续修订号、旧修订不可变、审计失败回滚和双请求并发冲突。
- [ ] 1.2 先为已完成记录创建人工修订编写后端失败测试，覆盖当前负责人/固定角色/产品范围/当前分配、修订原因必填与超限、历史或非 completed 状态、旧 `row_version`、额外控制字段和事务回滚。
- [ ] 1.3 先为 `returned`/`reassess` 编辑及重新提交编写回归测试，覆盖完整性与环境一致性校验、当前审核人非本人、人员重新分配、历史修订只读、父修订事件不变和重复提交冲突。
- [ ] 1.4 为历史列表、指定修订详情和同链版本比较编写授权与契约测试，覆盖稳定排序、范围外不泄露、跨链拒绝、纯文本字段和历史动作能力全禁用。
- [ ] 1.5 更新 OpenAPI 契约测试，固定创建修订、历史和比较接口，退回响应的已审核/新当前修订结构，以及详情中的父修订、当前标记、原因类型和动作能力。
- [ ] 1.6 重新生成前端 TypeScript API 类型并执行漂移检查，确保不手写重复的修订、历史或差异模型。

## 2. 数据访问与后端实现

- [ ] 2.1 在 repository 实现按业务键锁定当前修订、计算下一连续修订号、条件降级旧 current 和插入新 current 的统一原语，并集中定义业务快照复制、事件清空与系统字段重置白名单。
- [ ] 2.2 扩展退回服务与 `/return` 路由，在同一事务写原修订审核留痕、创建下一 `returned` 修订、切换 current 和写单条关联父子修订的受限审计，成功返回新当前详情及被审核修订摘要。
- [ ] 2.3 增加人工创建修订请求/响应 schema、服务和 `POST /api/v1/assessments/{assessment_id}/revisions`，仅允许当前指定负责人从当前 `completed` 修订创建带 `reassess_reason` 的 `reassess` 当前修订。
- [ ] 2.4 扩展草稿授权，使当前指定负责人可编辑当前 `returned`/`reassess` 修订，同时拒绝被退回父修订、已提交/完成修订、历史负责人及人员或范围已变化的请求。
- [ ] 2.5 扩展既有 `/submit` 接受当前 `returned`/`reassess`，复用全部最终校验、禁止自审、条件更新和审计，并确保只写当前修订的提交事件。
- [ ] 2.6 实现修订历史摘要、指定版本详情与同链字段白名单比较查询，统一复用有效产品范围，按修订号稳定排序且不给历史版本任何写动作能力。
- [ ] 2.7 为统一克隆白名单增加模型完整性测试，要求每个 `product_assessment` 非系统字段明确归类为复制、清空或重算，防止后续字段静默遗漏。
- [ ] 2.8 验证所有修订创建、当前切换和 `audit_log` 同事务提交，错误响应稳定区分 `403`、`409` 与 `422`，且审计不复制完整意见、原因、证据或结论正文。

## 3. 前端交互

- [ ] 3.1 扩展评估 API 客户端和详情状态适配，正确处理退回后返回的新当前 ID、父修订、原因类型、current 标记及创建修订/重新提交能力。
- [ ] 3.2 在评估详情增加修订历史时间线和返回当前版本入口，按需加载指定历史详情，明确标记当前/历史及提交审核事件，并保证历史表单完全只读。
- [ ] 3.3 实现退回后新当前修订的退回意见置顶、当前负责人编辑与重新提交确认，成功后完全采用服务端响应刷新状态、行版本和时间线。
- [ ] 3.4 为当前已完成修订实现“创建新修订”确认和修订原因输入，按服务端能力控制入口并跳转到返回的 `reassess` 当前修订。
- [ ] 3.5 实现同链两个修订的字段级比较，安全区分新增、删除、修改及业务/事件字段，所有长文本按不可信纯文本展示。
- [ ] 3.6 完成校验、人员/范围变化、并发冲突、跨链错误和服务失败反馈；失败时不猜测 current，不丢失未提交原因，并提供刷新当前修订路径。
- [ ] 3.7 增加前端组件测试，覆盖时间线排序与切换、历史只读、退回意见、创建修订、编辑重提、版本比较、请求防重及主要错误反馈。

## 4. 集成与端到端验证

- [ ] 4.1 使用 MySQL 集成测试验证 `submitted → returned(历史) + returned(当前) → submitted → completed` 完整链，断言旧结论和各次提交/审核留痕不被覆盖。
- [ ] 4.2 使用 MySQL 双会话验证并发退回、并发人工创建修订和创建期间保存/提交竞争均仅一个成功，始终只有一个 current、连续修订号和完整父链。
- [ ] 4.3 验证退回、人工创建、重新提交和审计写入任一步故障均整体回滚，历史/比较读取不写审计且范围外直接 ID 不泄露。
- [ ] 4.4 使用系统 Chrome 运行 Playwright，覆盖审核退回、负责人查看意见并修改重提、再次审核通过、完成记录人工修订、历史时间线和版本比较；不得下载 Playwright Chromium。
- [ ] 4.5 回归工作台 `returned/reassess/submitted` 实时计数与分配、评估草稿和 CVSS 环境评分、OTS-14 通过流程及产品范围授权，确认未新增通知、自动复评或跨产品参考行为。

## 5. 质量、文档与验收

- [ ] 5.1 运行后端 pytest（含 MySQL 专项）及覆盖率检查，确保新增后端代码覆盖率不低于 80%，并记录测试数量和关键模块覆盖率。
- [ ] 5.2 运行前端 Vitest、类型检查、生产构建及覆盖率检查，确保新增前端代码覆盖率不低于 80%；E2E 优先使用系统 Chrome，Chrome 不可用时检查 Edge。
- [ ] 5.3 连续生成两次 OpenAPI 并比较结果，确认契约与生成类型稳定且无漂移。
- [ ] 5.4 运行 `openspec validate ots-15-return-revision-resubmit --strict` 及项目全量 OpenSpec 严格校验。
- [ ] 5.5 按 FR-REVIEW-004、FR-ASSESS-009、产品评估状态设计和关键事务规则完成需求追溯，明确自动来源变化复评仍属 OTS-16、跨产品参考属 OTS-17、全局追溯属 OTS-19。
- [ ] 5.6 更新相关开发文档和 `doc/Task.md` 完成证据，记录实现提交、测试/构建结果、覆盖率、无需迁移的结论及剩余风险；仅在全部验收通过后同步规格并归档 change。
