## 1. 测试与契约夹具

- [x] 1.1 建立格式版本 `1.0` 的契约常量与确定性最小合规 ZIP 夹具，覆盖十个固定 CSV、manifest 三类记录、范围摘要和文件摘要，并验证相同输入生成稳定字节
- [x] 1.2 先编写 ZIP 安全失败测试，覆盖非法扩展名/文件名、损坏 ZIP、路径穿越、绝对/驱动器/反斜杠路径、目录、重复/未知/缺失文件、符号链接和非普通成员
- [x] 1.3 先编写资源限制测试，覆盖上传大小、成员数、单文件/总解压大小、压缩比、实际字节超限、CSV 行数、字段字节数和错误明细截断
- [x] 1.4 先编写 CSV 契约测试，覆盖 UTF-8 无 BOM、CRLF、精确表头顺序、转义、NUL、非法类型/枚举/时间、数据行号及所有十个文件的固定 Schema
- [x] 1.5 先编写 manifest 与摘要测试，覆盖 package/file/scope_result 唯一性、公共元数据一致性、文件清单闭包、范围 SHA-256、逐文件 SHA-256 和逐 OTS 采集状态规则
- [x] 1.6 先编写范围与引用测试，覆盖不可识别/重复/范围外 OTS、无 match CVE、评分/CWE/引用/KEV/受影响范围悬空引用、lifecycle 范围引用及候选语义
- [x] 1.7 先编写分类与错误输出测试，覆盖包内 new/duplicate/conflict/error、update 固定为零、有限样例、错误码/文件/行/字段/截断值和规范错误 CSV 字节

## 2. 包契约与纯校验器

- [x] 2.1 实现 `1.0` 十文件表头、主键/父键、公共字段类型、枚举、长度和兼容版本注册表，禁止相似列名或历史模板自动映射
- [x] 2.2 实现受限上传流与整包 SHA-256 计算，使用服务生成临时名并保证异常、取消、重复和失败路径清理临时文件
- [x] 2.3 实现 ZIP central directory 预检和逐成员受限读取，不调用路径型解压，验证普通文件、路径、精确文件集合、声明/实际大小及压缩比
- [x] 2.4 实现严格 CSV 字节和逐行解析，产生物理行号、规范化键、有限样例及不泄露敏感内容的结构化错误
- [x] 2.5 实现 manifest 三类行解析及批次、格式、生成时间、范围 ID/摘要、九个非 manifest 文件摘要和 scope_result 完整性校验
- [x] 2.6 实现范围快照校验与 OTS 批量可识别性查询，验证同一 `scope_export_id`、OTS 唯一性、manifest 范围一致性和 matches/lifecycle 范围约束
- [x] 2.7 实现领域键集合、包内重复/冲突分类和 CVE/OTS 跨文件引用闭包校验，确保候选 match 不转换为产品受影响结论
- [x] 2.8 实现汇总结果、文件统计、有限错误/样例、`classification_basis=package_structure_v1` 与 `final_import_diff=false`，并生成规范错误清单 CSV

## 3. 批次数据与后端 API

- [x] 3.1 为上传临时目录、验证包归档目录、50 MiB 上传、50 MiB 单文件、200 MiB 总解压、100:1 压缩比、10,000 行、64 KiB 字段和错误上限增加安全设置及 `.env.example` 校验
- [x] 3.2 实现 `ImportBatch` Repository 的整包摘要/批次号查重、`uploaded` 占位创建、`validated`/`failed` 原子结果更新、唯一键竞争恢复和按 ID 查询
- [x] 3.3 实现校验 Service 的阶段编排、事务边界、成功归档、失败清理和结构化日志，保证异常后只有可识别批次状态且不回显服务器路径
- [x] 3.4 定义上传、批次详情、分类、文件统计、错误和能力标识的 Pydantic Schema，并保持 `archive_path`、原始 CSV 行和完整包内容不进入响应
- [x] 3.5 新增管理员专用 `POST /api/v1/import-packages/validate`，对新批次返回 201、重复包/批次返回既有结果，并覆盖稳定文件校验错误码
- [x] 3.6 新增管理员专用 `GET /api/v1/import-packages/{batch_id}` 与错误清单下载端点，处理不存在、无权限、非失败状态和数据库/文件服务异常
- [x] 3.7 注册路由并生成 OpenAPI，复核 multipart 限制、统一错误 envelope、401/403 行为和前端 TypeScript 类型无漂移
- [x] 3.8 用后端 API/Repository 测试验证新包成功/失败、同摘要/同批次及并发重复上传、状态 JSON、归档/清理、无 `audit_log` 与无任何领域数据副作用
- [x] 3.9 使用 MySQL 8.x 验证既有 `import_batch` 唯一键、状态与 JSON 写入、失败恢复和 11 张应用基础表数量不变，确认本 change 无新增迁移脚本

## 4. 前端上传与校验预览

- [x] 4.1 增加基于生成类型的数据包 API client，支持 multipart 上传、批次详情读取、错误 Blob 下载及统一错误解析
- [x] 4.2 新增管理员路由 `/system/data-exchange/import-packages` 和“数据交换－数据包导入”入口，复用现有认证/管理员守卫并保持直接越权访问反馈
- [x] 4.3 实现“上传数据包、校验预览、确认导入、查看结果”四步向导骨架，仅启用前两步并明确后两步未开放且不发起写入请求
- [x] 4.4 实现单 ZIP 选择、客户端快速大小提示、上传进度/校验中、重新选择和新上传前清除旧结果，且不写 localStorage/sessionStorage
- [x] 4.5 实现全包与文件级 new/update/duplicate/conflict/error 卡片、有限样例、范围统计、“尚未正式写入”说明及空分类状态
- [x] 4.6 实现文件级与逐行错误表、错误清单下载、截断提示，以及权限、服务、网络和刷新读取失败状态，不显示服务器归档路径
- [x] 4.7 编写 API client 与页面组件测试，覆盖成功预览、损坏包、空统计、重复提示、错误下载、禁用确认导入、401/403、服务失败和旧结果清理

## 5. 纵向集成与非功能验证

- [x] 5.1 完成“管理员上传合规包 → 批次 validated → 查看分类预览”的后端集成与系统 Chrome Playwright 纵向场景
- [x] 5.2 完成“管理员上传摘要不符/范围外 OTS/无 match CVE/悬空引用包 → failed → 查看并下载精确错误”的纵向场景
- [x] 5.3 完成恶意 ZIP 纵向安全测试，确认路径穿越、符号链接、压缩炸弹和超限文件均在受控阶段被拒绝且临时目录无遗留
- [x] 5.4 验证未登录和非管理员对上传、详情、错误下载及页面均被拒绝，隐藏导航不作为安全边界且响应不泄露批次信息
- [x] 5.5 使用不超过 10,000 条领域数据行的合规包记录校验耗时与峰值内存，确认五分钟目标、错误/样例上限和零互联网访问
- [x] 5.6 验证上传、成功校验、失败校验、重复上传和预览/下载均不写 `audit_log`，不创建或修改漏洞、候选、覆盖或评估数据
- [x] 5.7 运行后端 pytest 与覆盖率、前端 Vitest 与覆盖率、类型检查和生产构建，确认前后端新增代码覆盖率均不低于 80%
- [x] 5.8 按项目约定使用系统 Chrome 运行完整 Playwright 套件；若 Chrome 不可用再检查 Edge，不下载 Playwright Chromium

## 6. 文档、追溯与验收

- [ ] 6.1 编写外部数据服务可执行的 `1.0` ZIP/CSV 契约文档与合规最小样例，说明十文件表头、manifest 行类型、摘要、主键/引用、编码、时间、空文件和兼容策略
- [ ] 6.2 更新后端/前端开发与部署文档，说明三个 API、管理员权限、文件目录、大小/超时设置、归档/备份、临时清理、同步校验和受控回滚，不记录真实凭据
- [ ] 6.3 对照 `FR-EXCH-008`、`FR-EXCH-012`、`FR-EXCH-013`、`FR-EXCH-014` 与 NFR 12.3 更新需求追溯和测试证据，明确包内一致性不等于历史签发证明
- [ ] 6.4 复核 OTS-08～10 边界：本 change 不做联网采集、领域正式写入、覆盖推进、评估任务或确认导入，并记录后续扩展同一格式和预览模型的约束
- [ ] 6.5 执行 OpenAPI 重复生成漂移检查，确保前端只消费生成类型且错误/下载响应契约一致
- [ ] 6.6 执行 `openspec validate ots-07-package-contract-and-validation --strict --no-interactive` 并修复全部校验错误
