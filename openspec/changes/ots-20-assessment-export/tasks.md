## 1. 失败测试与验收基线

- [x] 1.1 为导出范围增加后端失败测试，覆盖产品版本与 OTS 必填、从属关系、零行范围、全部当前状态、排除历史修订和稳定 CVE/评估 ID 次序。
- [x] 1.2 为产品范围授权增加后端失败测试，覆盖管理员全局访问、普通用户有效范围、混合越权对象、预检后撤权、未登录 `401`、越权 `403`、不存在 `404` 和非法参数 `422`，验证错误差异不泄露对象关系。
- [x] 1.3 为 CSV 契约增加字节级失败测试，覆盖 UTF-8 BOM、十九列中文表头、CRLF、空值、中文、逗号、双引号、换行、公式注入防护、安全文件名、数值与时间格式。
- [x] 1.4 为流式与只读边界增加失败测试，覆盖首批查询失败、传输中失败、客户端取消、数据库资源释放、有界内存，以及成功/失败/拒绝/取消均不写审计或改变评估数据。
- [x] 1.5 为评估导出页面增加组件失败测试和系统 Chrome 纵向 RED 场景，覆盖联动选择、预检确认、零行、重复点击、取消、错误体不保存为 CSV、撤权和失败重试。

## 2. OpenAPI 与 CSV 契约

- [x] 2.1 定义 preview 请求/响应 schema，固定 `product_version_id`、`ots_id`、安全范围摘要、`row_count` 和 `401/403/404/409/422` 错误语义。
- [x] 2.2 新增 `GET /api/v1/assessment-exports/preview` 与 `GET /api/v1/assessment-exports/csv` 路由契约，固定 `Content-Type`、`Content-Disposition` 和只读数据库会话。
- [x] 2.3 实现集中式十九列定义及格式化规则，固定 BOM、CRLF、CSV 转义、公式注入防护、空值、枚举、时间、数值和安全文件名。
- [x] 2.4 生成 OpenAPI 与前端 TypeScript 类型，连续生成两次并比较结果，确保契约和类型无手写漂移。

## 3. 数据查询、授权与后端流式导出

- [x] 3.1 实现共享范围解析服务，复用有效产品版本授权并按“先范围、后从属”顺序校验，确保 preview 与 csv 各自重新授权且不产生侧信道。
- [x] 3.2 实现轻量只读投影查询，选择指定 `product_ots` 下全部状态的当前修订及十九列所需数据，排除历史修订、跨产品数据和候选推断。
- [x] 3.3 使用 `(cve_id, assessment_id)` keyset 分批读取，在同一只读一致快照中保证稳定集合和次序，禁止 ORM 懒加载、逐行补查与全量内存列表。
- [x] 3.4 实现 preview 行数与范围摘要；合法零行返回 `200`，csv 对零行返回 `409 export_empty`，并保证两端点复用相同集合定义。
- [x] 3.5 实现 CSV 生成器，在响应提交前完成授权与首批查询，逐批输出并在客户端断开或异常时关闭结果集和数据库会话，不创建文件、任务或导出记录。
- [x] 3.6 使第 1.1～1.4 节后端测试转绿，运行相关 pytest 与覆盖率检查，确保新增后端代码覆盖率不低于 80%。

## 4. 前端评估导出页面

- [x] 4.1 新增“数据交换－评估导出”路由和导航入口，并只通过现有授权 API 加载当前用户可访问的产品版本及版本关联 OTS。
- [x] 4.2 实现产品版本与 OTS 联动选择、选择变化后清除旧预检、加载状态、字段校验及合法零行空状态。
- [x] 4.3 实现导出确认区，显示产品、版本、OTS、预检时间与行数，并明确下载以实际请求时快照为准。
- [x] 4.4 实现文件下载、进行中禁用、防重复请求、取消和完成反馈；校验状态码与响应类型，确保 JSON 错误体不会保存为 `.csv`。
- [x] 4.5 映射 `401/403/404/409/422`、服务和网络错误，失败后保留选择并允许重新预检或重试，授权失效时不得沿用旧确认结果。
- [x] 4.6 使第 1.5 节前端组件测试转绿，并运行 Vitest/coverage、`npm run typecheck` 和生产构建，确保新增前端代码覆盖率不低于 80%。

## 5. MySQL、性能与纵向集成验证

- [x] 5.1 在真实 MySQL 8 建立同一 OTS 跨产品、全部当前状态、历史修订、空字段、特殊字符和混合授权数据，核对 preview 数量及 CSV 每个单元格与数据库事实一致。
- [x] 5.2 在导出期间并发创建新修订或改变当前标记，验证单次下载使用一致读取、不重复不漏行且不出现同一链多个当前修订。
- [x] 5.3 对范围计数和 keyset 批次 SQL 执行 `EXPLAIN`/`EXPLAIN ANALYZE`，记录执行计划与耗时；仅在证据表明必要时评估既有表最小索引，不新增第 12 张表。
- [x] 5.4 使用代表性大结果验证首字节时间、总耗时、峰值内存、事务持续时间和取消后的资源释放，并确认服务端内存不会随总行数线性增长。
- [x] 5.5 使用系统 Chrome 使纵向场景“选择范围 → 预检确认 → 流式下载 → 校验 CSV”转绿，并覆盖零行、撤权、特殊字符、重复点击、取消和失败重试；不得下载 Playwright Chromium。

## 6. 回归、文档与交付检查

- [x] 6.1 回归 OTS-05 产品范围、OTS-14 提交审核、OTS-15 修订链和 OTS-19 查询追溯，确认导出不改变状态、修订、行版本、待办或 `audit_log`。
- [x] 6.2 运行完整后端 pytest/coverage、前端 Vitest/coverage、类型检查、生产构建、OpenAPI 漂移检查及系统 Chrome Playwright 回归，并保留任何环境阻塞的实际错误。
- [x] 6.3 验证数据库仍为 11 张应用基础表，未新增迁移、后台任务、临时文件、持久化导出记录、消息队列或新依赖；记录部署与无数据迁移回滚检查结果。
- [x] 6.4 更新 `doc/Task.md` 的 OTS-20 状态、范围、完成证据和需求追溯，按 FR-EXPORT-001、FR-EXPORT-002、FR-AUDIT-003 及关键验收规则 17 建立双向证据。
- [x] 6.5 运行 `openspec validate ots-20-assessment-export --strict --no-interactive` 及 OpenSpec 全量严格校验，确保 proposal、spec、design、tasks 一致且无错误。
- [ ] 6.6 仅在全部验收通过后同步主规格、归档 change 并提交到当前主干，使用中文提交说明且不执行 `git push`。
