## 1. 后端 RED 测试与数据边界

- [x] 1.1 在评估后端测试中先构造相同 OTS/CVE 的多产品、多版本、混合状态和父子修订数据，写出仅返回其他产品当前 `completed/approved` 修订的失败测试，并覆盖同一产品其他版本排除与历史批准不回退。
- [x] 1.2 先写字段白名单与安全失败测试，精确断言响应 key 只含产品名称、产品版本、适用性、分析摘要、环境分数、处置方式和审核完成时间，且无权入口不泄露目标、参考或数量信息。
- [x] 1.3 先写纯读取测试，记录查询前后的目标/参考评估、`row_version`、修订数、待办数和 `audit_log` 数量，覆盖成功、空集合、拒绝及服务失败均无业务写入。
- [x] 1.4 核对既有 11 表的外键与索引可支撑目标查询，记录无需新增表、字段、索引或迁移；以 MySQL 集成数据验证按产品 ID 排除而非按版本或 `product_ots_id` 排除。

## 2. 后端查询、授权与 API

- [x] 2.1 在评估 repository 实现显式列投影查询，通过目标产品 OTS 取得 `ots_component_id`，按相同 `vulnerability_id`、其他产品、当前修订、完成状态和批准决定筛选，并按审核完成时间、产品名称和版本稳定排序。
- [x] 2.2 在评估 service 复用目标评估详情的认证、产品范围与资源隐藏语义作为入口授权，授权通过后仅调用专用跨范围摘要查询，不暴露参考评估 ID 或详情/写能力。
- [x] 2.3 定义严格的参考摘要响应 schema，并实现 `GET /api/v1/assessments/{assessment_id}/approved-references`；环境分数缺失保持空值，空结果返回成功空集合。
- [x] 2.4 运行后端专项测试和完整 pytest/覆盖率，确保新增代码覆盖率不低于 80%，并对 MySQL 混合修订查询执行实际集成验证。

## 3. OpenAPI 与前端 RED 测试

- [x] 3.1 生成并检查 OpenAPI 中的专用只读响应契约，重新生成 TypeScript 类型；连续生成两次并比较结果，确认无契约漂移且 schema 不含任何非白名单字段。
- [x] 3.2 在 `assessmentEditor` API 测试中先覆盖成功、空值、空集合和错误响应，验证请求固定使用评估 ID 子资源且不提交 OTS、CVE、产品或表单数据。
- [x] 3.3 在评估详情组件测试中先覆盖独立加载、成功多条、明确空状态、环境分数“未提供”、失败重试、路由切换清空旧摘要及无复制/套用/详情跳转控件。
- [x] 3.4 增加参考查看与刷新不改变当前表单未保存输入、详情错误、冲突状态或保存/提交/审核能力的回归测试。

## 4. 前端只读参考区域

- [x] 4.1 在前端评估 API 层接入生成类型并实现只读参考查询，保持参考请求状态与当前评估详情及动作请求状态相互独立。
- [x] 4.2 在 `AssessmentDetailPage.vue` 的候选匹配与当前产品结论之间实现“其他产品参考”区域，展示白名单字段、“仅供参考”声明以及加载、失败重试和空状态。
- [x] 4.3 确认参考卡片没有向评估草稿赋值的处理器、没有其他产品详情链接，也不因参考失败禁用既有保存、提交或审核操作；补齐窄屏与只读状态样式验证。
- [x] 4.4 运行前端 Vitest、覆盖率、TypeScript 类型检查和生产构建，确保新增代码覆盖率不低于 80% 且既有评估编辑、提交、退回、修订和复评测试继续通过。

## 5. 纵向验收与交付检查

- [x] 5.1 使用系统 Chrome 运行 Playwright 纵向场景，验证产品负责人和当前指定审核人均能在同页查看参考，混合状态/历史修订/同产品其他版本不可见，且无合格记录时显示明确空状态。
- [x] 5.2 在 E2E 网络响应中精确检查字段白名单，并验证查看、刷新和失败重试后当前任务状态、行版本、待办数量、修订数与审计记录均不改变。
- [x] 5.3 运行后端完整 pytest、前端完整 Vitest/覆盖率、类型检查、生产构建、OpenAPI 漂移检查以及系统 Chrome Playwright 全量回归，保留实际命令与结果。
- [x] 5.4 执行 `openspec validate ots-17-cross-product-approved-reference --strict` 及全量 OpenSpec 严格校验，确认 proposal、design、delta spec 和 tasks 一致且未修改主规格。
- [x] 5.5 按 FR-ASSESS-011、FR-REVIEW-001、关键业务规则 17 和 V1 验收项 13 完成需求追溯，更新 `doc/Task.md` 的完成证据；明确全局查询/分页属 OTS-19、评估导出属 OTS-20、跨产品复制仍为 V1 后置能力。
