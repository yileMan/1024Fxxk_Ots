## 1. 测试基线与评分契约

- [ ] 1.1 从 FIRST CVSS v3.1 官方资料整理可追溯的基础向量、环境指标、预期分数与完整向量夹具，加入 Modified Scope、`MPR`、零影响、10 分上限和 Roundup 临界用例
- [ ] 1.2 先编写后端失败测试，锁定 v3.1 向量解析、11 个环境指标枚举、`X` 回退、确定性结果、非法/重复/缺失指标及 v4.0 拒绝行为
- [ ] 1.3 先编写评估 API/服务失败测试，覆盖无来源与非法来源、客户端派生字段篡改、完整草稿原子保存、权限、范围撤销、只读状态、无变化保存和旧 `row_version` 冲突
- [ ] 1.4 先编写前端计算与组件失败测试，覆盖官方夹具、来源提示、11 个指标控件、未保存预览、服务端结果回填、校验错误、只读展示和并发冲突输入保留

## 2. 后端 CVSS v3.1 计算核心

- [ ] 2.1 实现只接受完整 `CVSS:3.1` 基础向量的严格解析与规范化，拒绝未知、重复、缺失、非法值和其他 CVSS 版本
- [ ] 2.2 实现 `CR/IR/AR/MAV/MAC/MPR/MUI/MS/MC/MI/MA` 闭集校验、完整 `X` 规范化和稳定字段路径错误
- [ ] 2.3 按 FIRST v3.1 公式实现环境分数、规范化完整向量和 `ots-cvss31-1` 计算器版本，确保 Modified Scope、`MPR` 权重、影响上限及 Roundup 与官方预期一致
- [ ] 2.4 运行计算核心专项测试并确认所有官方与边界夹具通过，且相同输入重复计算结果一致

## 3. API 契约与生成类型

- [ ] 3.1 扩展评估详情 schema，返回来源 v3.1 信息、环境评分可用状态/稳定原因、规范化指标、已保存分数、向量和计算器版本，不暴露 v4.0 字段
- [ ] 3.2 扩展完整草稿更新 schema 接收结构化 `cvss_metrics` 并禁止额外派生字段，定义非法指标和来源不可用的 `422` 错误码及字段路径
- [ ] 3.3 更新 OpenAPI 契约并重新生成 TypeScript 类型，连续生成两次确认产物无漂移

## 4. 数据与后端草稿集成

- [ ] 4.1 扩展评估详情查询/映射，读取当前来源 v3.1 与既有五个环境评分字段，并对无来源、非法来源及历史修订返回正确只读语义
- [ ] 4.2 在保存事务中使用数据库当前来源向量权威重算，将 `cvss_version`、指标 JSON、分数、向量和计算器版本作为一致性组加入既有条件更新
- [ ] 4.3 将评分组纳入规范化无变化比较，保证核心草稿与环境指标任一变化只递增一次 `row_version`，完全相同保存不更新版本、时间或审计
- [ ] 4.4 扩展 `product_assessment` 更新审计，记录指标键、服务端分数前后值、向量摘要、计算器版本和行版本，并验证失败事务不留下部分评分或审计
- [ ] 4.5 使用 MySQL 集成测试验证 JSON 与 `DECIMAL(3,1)` 持久化、双 session 并发、权限/范围复核、审计原子性和全部 `X` 保存，不新增表、字段、索引或迁移

## 5. 前端环境评分交互

- [ ] 5.1 实现浏览器端 v3.1 纯计算预览并复用官方与边界数据夹具，确保与后端专项结果一致
- [ ] 5.2 在评估详情当前产品结论区展示来源分数、严重度、向量和评分来源，并为来源缺失/非法状态展示明确提示且不渲染评分表单
- [ ] 5.3 为可编辑评估实现 11 个环境指标控件、`X` 默认语义、未保存预览标签、分数与完整向量展示，并通过既有明确保存流程提交指标
- [ ] 5.4 保存成功后使用服务端响应重置指标、已保存结果和 `row_version`；校验、权限、服务及并发失败时保留输入并区分预览与已保存值
- [ ] 5.5 为只读或历史修订展示持久化指标、分数、向量和计算器版本，禁止编辑、保存及读取时静默重算

## 6. 集成、回归与覆盖率

- [ ] 6.1 运行后端 CVSS、评估编辑、漏洞来源兼容与 MySQL 集成测试，再运行后端全量 pytest 和覆盖率检查，确认新增代码覆盖率不低于 80%
- [ ] 6.2 运行前端 Vitest/组件全量测试、TypeScript 类型检查和生产构建，确认前端计算与服务端官方夹具结果一致且新增代码覆盖率不低于 80%
- [ ] 6.3 使用系统 Chrome（`PLAYWRIGHT_CHANNEL=chrome`）运行 Playwright，覆盖负责人预览/保存/重载、无来源提示、只读修订和篡改请求无效；若 Chrome 不可用则检查系统 Edge并保留实际环境错误
- [ ] 6.4 验证既有核心草稿保存、待办/漏洞详情入口、授权裁剪、乐观锁和审计回归均通过，且无 v4.0 API、页面或数据库写入

## 7. 文档与验收

- [ ] 7.1 更新相关 API/开发文档，说明 11 个环境指标、`X` 语义、来源前置条件、服务端权威计算、计算器版本和无数据库迁移/回滚方案
- [ ] 7.2 对照 FR-MASTER-007、FR-ASSESS-005、FR-VULN-002、FR-VULN-003、系统方案第 6 章及两个 delta spec 建立需求—测试证据追溯
- [ ] 7.3 运行 OpenAPI 漂移检查和 `openspec validate ots-13-cvss31-environmental-scoring --strict`，并修复全部契约或规范错误
- [ ] 7.4 汇总后端、前端、构建、Playwright、覆盖率、官方示例、11 表约束和 v4.0 非目标证据；仅在全部通过后进入归档准备
