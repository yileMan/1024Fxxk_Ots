## 1. 测试基线与变化矩阵

- [x] 1.1 先为规范基线与差异引擎编写失败测试，固定状态、CVSS v3.1 十进制分数/规范向量、受影响范围、候选存在性/方式/证据、条件 KEV 和产品 OTS 状态的稳定 JSON 与 SHA-256。
- [x] 1.2 补充非实质变化失败测试，覆盖描述、CWE、引用、来源/批次时间、JSON 键顺序、等价空白和未启用 KEV 缺失值均不产生评估写入、行版本或审计。
- [ ] 1.3 为 `completed` 自动复评编写失败测试，覆盖业务快照继承、事件字段清空、当前负责人、连续父子链、唯一 current、原因类型、变化摘要、基线和工作台可见性。
- [ ] 1.4 为 `pending/returned/reassess/submitted` 编写失败测试，覆盖状态与用户内容不变、变化按字段合并/回退消除、基线更新、`row_version` 递增及旧保存/提交/审核请求返回 `409`。
- [ ] 1.5 为跨入口幂等编写失败测试，覆盖相同包、不同批次相同内容、来源后候选重算、关联重试和同一 `reassess` 上连续变化不重复修订或审计。
- [ ] 1.6 为产品 OTS 新增、停用、恢复与无历史删除编写失败测试，覆盖缺失任务生成、仅本产品受影响、软状态、唯一关联、乐观锁、CSV 遇到 disabled 冲突和采集范围过滤。
- [ ] 1.7 为权限和安全响应编写失败测试，覆盖非管理员关联写入、范围外变化摘要、未登录访问、有界/脱敏前后值、负责人不可用和事务失败零泄露零审计。

## 2. 数据迁移与初始化

- [x] 2.1 新增 `013_automatic_reassessment.sql`，为 `product_assessment` 添加可空基线哈希、基线 JSON、变化摘要 JSON，为 `product_ots` 添加 `status`、CHECK、`row_version` 及必要索引，保持应用基础表总数为 11。
- [x] 2.2 新增对应回滚说明，明确启用自动触发后的基线、软停用关联和复评历史不得自动删除，只有未启用且无数据依赖时才能移除新增列。
- [x] 2.3 更新 SQLAlchemy 模型和迁移结构测试，验证 MySQL 8.4 字段类型、默认值、CHECK、索引、外键和既有唯一键，并确认 SQLite 测试兼容路径不弱化业务校验。
- [ ] 2.4 实现可重入、稳定分页的当前评估基线初始化命令与 dry-run 摘要；验证只填充空基线，不创建修订、不改变产品结论或状态，并在完整初始化前阻止自动触发写入。
- [ ] 2.5 在 MySQL 副本验证初始化重跑幂等、处理中断可恢复、批量内存上限和新旧应用兼容检查，记录上线前置条件。

## 3. OpenAPI 契约与后端核心

- [x] 3.1 先更新后端 schema 和 OpenAPI 契约测试，固定 `reassessment` 摘要、产品 OTS 状态/行版本、停用/恢复请求响应、稳定错误码和禁止额外控制字段。
- [x] 3.2 实现评估基线规范化、稳定编码、指纹和字段级 diff，复用既有版本范围与 CVSS v3.1 解析语义，并限制变化条目、显示值长度与敏感字段白名单。
- [x] 3.3 实现变化摘要合并规则：按字段路径稳定排序，保留起点 before 与最新 after，值回到起点时删除变化项，并正确维护触发类型、时间和截断计数。
- [ ] 3.4 在 repository 实现按业务键稳定排序锁定当前评估、条件更新进行中基线/摘要及行版本，保证无变化零写入并为冲突提供统一重算入口。
- [ ] 3.5 抽取 OTS-15 统一修订克隆原语，集中管理复制、清空、重算字段；迁移 OTS-10 自动复评和人工/退回路径使用同一原语并扩展模型字段完整性测试。
- [ ] 3.6 停止通过扫描 `audit_log` 推断原因类型，改为从修订持久化触发元数据序列化，同时保持现有人工修订和退回历史响应兼容。
- [x] 3.7 重新生成前端 TypeScript API 类型并执行漂移测试，确保变化摘要和关联状态模型只来源于 OpenAPI。

## 4. 来源、候选与产品 OTS 事务集成

- [ ] 4.1 扩展导入预览，基于旧漏洞事实和现有候选计算 CVSS、状态、范围及条件 KEV 的复评/合并/未变统计，并在负责人不可用时提供稳定阻断原因且保持零写入。
- [ ] 4.2 扩展确认导入事务，在漏洞 upsert 前保留旧事实并调用差异引擎，使来源写入、评估操作、批次结果与受限审计原子提交，纯文字变化只更新漏洞事实。
- [ ] 4.3 重构候选任务规划，使用完整目标基线处理候选新增、更新、移除和现有任务补齐，删除旧的独立候选哈希复评分支，并保持既有预览/执行结果兼容。
- [ ] 4.4 更新候选执行事务和摘要，确保来源阶段已处理的范围变化不重复，只有额外候选差异才合并变化，失败时候选、评估、基线、结果和审计全部回滚。
- [x] 4.5 扩展产品 OTS repository/service：当前查询默认过滤 `active`，新增关联批量读取当前候选并创建带基线的缺失 `pending`，有历史移出改为条件停用，恢复复用同一关联。
- [ ] 4.6 新增关联 `/disable`、`/restore` 动作并保留无历史 `DELETE`；校验管理员、版本归属、`row_version`、负责人资格和状态转换，确保关联、任务/复评和审计同事务。
- [ ] 4.7 更新 CSV 导入、导出、关联产品查询和采集范围：只把 `active` 视为当前，disabled 重复导入返回稳定冲突且不静默恢复，历史管理查询可显式包含 disabled。
- [ ] 4.8 统一三个入口的受限审计与日志，只记录入口/批次/关联、数量、变化类型、父子修订和指纹，不记录完整前后值、证据、产品正文、Cookie 或绝对路径。

## 5. 评估详情与前端交互

- [x] 5.1 扩展评估详情、历史摘要和前端 API 适配，返回并解析当前修订的自动复评原因、基线指纹、变化类型、字段级摘要、触发时间和截断数。
- [x] 5.2 在评估详情顶部实现变化卡片，区分来源、候选和产品上下文，安全展示前后值，并为自动 `reassess` 提供父修订旧结论对比和既有重新提交入口。
- [x] 5.3 对 `submitted` 的提交后变化增加显著审核提示；所有保存、提交和审核冲突保留本地输入，刷新后采用服务端最新 `row_version`、摘要和动作能力。
- [x] 5.4 更新产品 OTS 清单，默认显示 active，提供历史关联视图、停用/恢复确认、软停用影响说明和并发冲突反馈；无历史删除仍使用原确认路径。
- [ ] 5.5 更新 CSV 冲突、加载、空状态、403/409/422 和事务失败反馈，失败时不从当前清单误移除关联、不显示任务或复评成功。
- [x] 5.6 增加前端组件测试，覆盖待复评卡片/列表跳转、变化摘要排序与截断、历史只读、提交后提示、重新提交、关联停用/恢复、请求防重和纯文本安全渲染。

## 6. 集成、并发与性能验证

- [ ] 6.1 使用 MySQL 集成测试验证 `completed → reassess` 的 CVSS、Rejected、范围、候选和关联状态矩阵，以及多产品各自独立、父修订不可变、唯一 current 和连续修订号。
- [ ] 6.2 使用 MySQL 双会话验证来源导入、候选执行、关联停用/恢复与保存/提交/审核竞争，确保仅一个写入成功或重算为未变，始终无多个 current、断链或丢失摘要。
- [ ] 6.3 注入评估克隆、基线更新、关联写入、批次结果和审计故障，验证各入口完整回滚；负责人不可用在预览阻断并在竞争条件下仍不产生部分来源更新。
- [ ] 6.4 回归 NVD `1.0` 未启用 KEV/EOL、OTS-08 候选、OTS-10 任务、OTS-11 工作台、OTS-13 环境评分、OTS-14 审核和 OTS-15 修订历史，确认没有包契约或结论语义漂移。
- [ ] 6.5 扩展 10,000 CVE 性能测试和代表性多产品/变化摘要数据，验证集合批量加载、锁顺序、处理时间与峰值内存，不出现逐评估 N+1。
- [ ] 6.6 使用系统 Chrome 运行 Playwright 纵向场景：已完成评估后导入实质变化、工作台进入待复评、查看摘要/父结论、修改重提审核，以及关联停用/恢复；不得下载 Playwright Chromium。

## 7. 质量、文档与验收

- [ ] 7.1 运行后端 pytest（含 MySQL、事务、并发和性能专项）及覆盖率检查，确保新增后端代码覆盖率不低于 80%，记录用例数量、关键模块覆盖率、耗时和峰值内存。
- [ ] 7.2 运行前端 Vitest、类型检查、生产构建及覆盖率检查，确保新增前端代码覆盖率不低于 80%；系统 Chrome 不可用时检查 Edge 并保留真实环境错误。
- [ ] 7.3 连续生成两次 OpenAPI 与 TypeScript 类型并比较哈希，确认契约稳定且无手写重复模型或漂移。
- [ ] 7.4 运行 `openspec validate ots-16-automatic-reassessment --strict` 和项目全量 OpenSpec 严格校验，修复所有 delta、场景与主规格兼容问题。
- [ ] 7.5 按 FR-VULN-005、FR-MATCH-005、FR-REVIEW-004、FR-WORK-001 和系统方案 5.5 完成需求追溯，逐项记录实质/文字变化、幂等、历史保护、独立产品和站内通知证据。
- [ ] 7.6 更新数据结构、系统方案、运维/初始化说明及 `doc/Task.md` 完成证据，记录 `013` 迁移、无新增基础表、KEV/EOL 条件边界、测试构建结果和剩余风险；仅在全部验收通过后同步规格并归档 change。
