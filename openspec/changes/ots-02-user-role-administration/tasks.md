## 1. 前置条件与失败测试

- [x] 1.1 确认 `ots-01-local-authentication` 已归档，复用 `app_user`、`audit_log`、当前用户解析、密码摘要服务、编号迁移与 API 类型生成；前置条件不满足时停止实施
- [x] 1.2 依据 `user-role-administration` Spec 先添加后端失败测试，覆盖未登录/非管理员拒绝、角色白名单、重复登录名、分页筛选、并发冲突、停用幂等和无秘密泄露
- [x] 1.3 先添加 MySQL 集成测试，覆盖条件更新、用户和审计同事务提交、审计失败回滚及失败操作不写审计
- [x] 1.4 先添加前端组件测试，覆盖列表加载/空/错误、角色多选、表单字段错误、停用确认、403 反馈和并发冲突保留输入

## 2. 契约与数据边界

- [x] 2.1 定义 `/api/v1/users` 的分页/筛选、详情、创建、编辑、重置密码和停用 OpenAPI 契约，固定公开 DTO、`row_version` 和稳定错误码
- [x] 2.2 生成并校验前端 TypeScript API 类型，禁止维护独立的用户/角色请求响应模型
- [x] 2.3 复核现有 `app_user` 的角色 JSON 约束、状态、唯一登录名和 `row_version`，以及 `audit_log` 的索引和动作约束；不新增表或字段

## 3. 后端用户与审计实现

- [x] 3.1 实现基于 OTS-01 当前用户的 `admin` 授权依赖，并让所有用户管理 API 在业务读写前执行服务端授权
- [x] 3.2 实现用户 Repository 的分页/筛选、公开详情和 `id + row_version` 条件更新，正确区分不存在与并发冲突
- [x] 3.3 实现创建用户 Service：校验唯一登录名、非空去重固定角色白名单，复用密码摘要服务并创建初始版本为 1 的活动用户
- [x] 3.4 实现编辑用户资料/角色、重置密码和停用 Service；已停用用户重复停用不改变版本或产生审计
- [x] 3.5 实现事务内审计服务调用，记录创建/更新/密码重置/停用的关键非秘密字段；保证审计失败或业务失败均回滚且不产生半成品
- [x] 3.6 实现路由、Schema 与统一错误映射，确保所有响应、异常和应用日志均不包含密码或密码摘要

## 4. 前端系统管理用户页面

- [x] 4.1 新增系统管理用户路由和仅管理员可见的导航入口；API 403 显示明确权限反馈，不以隐藏入口替代授权
- [x] 4.2 实现服务器分页和筛选的用户列表，覆盖加载、空结果、失败重试、创建入口和编辑入口
- [x] 4.3 实现创建/编辑表单，提供固定角色多选、字段错误和提交状态；密码输入仅用于创建或独立重置流程且不回显/缓存
- [x] 4.4 实现重置密码交互、停用二次确认及成功后的列表/详情同步
- [x] 4.5 处理 `USER_VERSION_CONFLICT`：保留未保存输入、提示刷新并获取最新详情，且不得展示为成功

## 5. 集成与验收

- [x] 5.1 运行后端单元与 MySQL 集成测试，验证管理员操作、越权拒绝、固定角色、分页、并发、停用和审计场景，且后端新增代码覆盖率不低于 80%
- [x] 5.2 运行前端组件测试、类型检查与生产构建，验证 API 类型无漂移且前端新增代码覆盖率不低于 80%
- [x] 5.3 编写并运行 Playwright 纵向场景：管理员创建多角色用户、编辑、重置密码、停用与冲突提示；非管理员直接 API 请求被拒绝
- [x] 5.4 在真实事务验证中确认成功写操作各有一条脱敏审计，验证/权限/冲突/回滚失败不产生审计，且审计、响应和日志不含密码或摘要
- [x] 5.5 执行 `openspec validate ots-02-user-role-administration --strict --no-interactive` 并修复全部校验问题

## 6. 文档与追溯

- [x] 6.1 更新使用说明，说明管理员维护用户、固定角色含义、停用保留历史和并发刷新重试方式，不写入真实凭据
- [x] 6.2 复核 FR-USER-001、FR-USER-002、FR-USER-007、FR-AUDIT-001、FR-AUDIT-002 的实现和测试证据，并标注产品授权与人员分配继续由 OTS-03/OTS-05 完成

## 7. 追加退出登录、左侧导航与主题升级

- [x] 7.1 先添加后端失败测试，覆盖 `POST /api/v1/auth/logout` 在有效、缺失和无效 Cookie 下均幂等清除 `ots_user_id`，且不查询业务数据、不写 `audit_log`
- [x] 7.2 先添加前端组件和 Playwright 失败测试，覆盖左侧三个导航项的名称/顺序/选中态、退出后返回登录页、可重新登录及非管理员 403
- [x] 7.3 实现退出 API 与 Cookie 清除响应，保持无服务端会话、无额外认证校验和无数据库审计
- [x] 7.4 更新 OpenAPI、前端认证客户端与内存身份状态，处理退出成功、失败反馈和路由跳转
- [x] 7.5 将认证后应用外壳改为左侧导航和右侧内容区，左栏严格显示“工作台、用户与角色、运行状态”，并加入当前用户摘要及退出入口
- [x] 7.6 按红/白/深灰/冷灰医疗科技方向更新全局主题、登录页、工作台、用户页、运行状态和权限页，验证响应式、键盘焦点与减少动效
- [x] 7.7 更新前后端说明，记录退出只影响当前浏览器、三项导航顺序和主题变量来源边界
- [x] 7.8 运行后端/前端覆盖率、类型检查、生产构建、API 漂移检查、完整 Playwright、视觉截图检查和 OpenSpec strict validation

## 8. 归档

- [ ] 8.1 汇总严格校验、测试、覆盖率、构建、迁移检查和 Playwright 证据；全部通过后归档 change 并更新 `doc/Task.md` 状态及完成提交号
