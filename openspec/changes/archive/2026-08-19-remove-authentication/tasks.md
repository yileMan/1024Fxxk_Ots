## 1. 测试与契约准备

- [x] 1.1 为用户名和密码匹配/不匹配、未签名用户 ID Cookie 以及不检查账号状态、密码规则和请求来源补充失败测试。
- [x] 1.2 更新 FastAPI OpenAPI：保留登录与 `/api/v1/auth/me`，移除退出端点和 Cookie 签名/过期相关响应约束。
- [x] 1.3 依据更新后的 OpenAPI 重新生成前端 TypeScript 类型，保留登录和当前用户客户端调用，并移除退出调用。

## 2. 后端与数据处理

- [x] 2.1 调整初始化用户与登录服务，使登录只依据登录名和密码摘要匹配，不再校验账号状态、密码长度/复杂度或来源。
- [x] 2.2 将认证 Cookie 改为未签名的用户 ID Cookie；保留当前用户解析以支持既有授权与审计，删除会话令牌、退出和来源校验实现及其认证专用配置。
- [x] 2.3 移除登录对角色和账号状态的校验依赖，保留 `app_user` 表、密码摘要、既有数据及当前用户响应中的角色，不创建删除表或数据的迁移。
- [x] 2.4 更新配置示例和运行文档，删除认证密钥、Cookie、会话、账号状态和来源校验说明。

## 3. 前端登录流程

- [x] 3.1 保留登录页；登录成功后写入用户 ID Cookie 并跳转业务页面，首次加载通过当前用户接口恢复身份。
- [x] 3.2 实现轻量路由守卫：需要登录的页面在未取得当前用户时跳转 `/login` 并保留站内目标地址；保留身份恢复，删除退出入口和认证附加处理。
- [x] 3.3 更新组件与 Playwright 测试，验证未登录访问跳转、登录后返回目标页面及用户 ID Cookie 身份恢复。

## 4. OpenSpec 与验收

- [x] 4.1 更新 `openspec/config.yaml`，写入“登录仅校验用户名密码并使用用户 ID Cookie 传递身份，后续不得新增 Cookie 签名、时效、令牌、来源、账号状态或其他认证附加机制”的长期约束。
- [x] 4.2 更新 `local-authentication` 主规格，使其包含轻量路由守卫并与本 change 的简化登录 delta 一致。
- [x] 4.3 更新 `doc/Task.md` 与受影响需求追溯，明确登录范围和已移除的认证附加机制。
- [x] 4.4 运行 `openspec validate remove-authentication --strict`，并运行后端 pytest、前端 Vitest、前端构建和 Playwright 冒烟测试；确认新增或改动代码覆盖率满足 80% 要求。
