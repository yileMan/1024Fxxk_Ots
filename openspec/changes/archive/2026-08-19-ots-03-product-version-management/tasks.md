## 1. 测试与数据契约准备

- [x] 1.1 为产品和产品版本的管理员授权、唯一性、人员资格、乐观锁、停用与审计场景编写后端失败测试。
- [x] 1.2 为产品建档向导、版本维护、人员选择、停用确认和并发冲突保留输入编写前端组件测试。
- [x] 1.3 为管理员完整建档流程、四项导航顺序与非管理员拒绝访问编写 Playwright 冒烟测试。

## 2. 数据库与后端实现

- [x] 2.1 新增编号 SQL 迁移及 rollback 文档，创建符合 11 表基线的 `product`、`product_version` 表、外键、唯一键和索引。
- [x] 2.2 实现产品和产品版本 SQLAlchemy 模型、仓储及分页、筛选和详情查询。
- [x] 2.3 实现产品与版本服务层，校验管理员操作、产品编号和产品内版本号唯一性、人员启用状态与角色，以及 `row_version` 乐观锁。
- [x] 2.4 实现产品和版本创建、更新、停用的同事务审计，保证失败、冲突和权限拒绝不产生审计记录，也不提供物理删除。
- [x] 2.5 新增 `/api/v1/products` 与版本子资源的 FastAPI Schema、路由和稳定错误映射，并接入应用。
- [x] 2.6 生成并检查 OpenAPI 契约，更新前端生成类型和客户端，确认不存在手写重复模型。

## 3. 前端实现

- [x] 3.1 将认证后左侧导航扩展为“工作台、产品管理、用户与角色、运行状态”四项；仅管理员显示并可访问“产品管理”，并新增对应路由。
- [x] 3.2 实现产品列表的分页、筛选、加载/空/错误状态及创建、编辑、停用交互。
- [x] 3.3 实现产品建档向导第 1～2 步，按顺序保存产品基本信息和首个产品版本。
- [x] 3.4 实现版本维护与负责人、审核人选择；仅展示启用且具备对应角色的候选人员，并处理字段校验。
- [x] 3.5 实现产品和版本编辑冲突提示，保留未保存输入并支持读取最新数据后重试。

## 4. 集成验证与验收

- [x] 4.1 运行后端 pytest 和 MySQL 集成测试，验证产品编号与同产品版本号唯一、人员资格、停用历史保留、审计原子性和并发冲突。
- [x] 4.2 运行前端 Vitest、类型检查、生产构建与 Playwright，验证建档、版本维护、权限拒绝和冲突交互。
- [x] 4.3 检查新增代码覆盖率不低于 80%，并执行 OpenAPI 漂移检查。
- [x] 4.4 执行 `openspec validate ots-03-product-version-management --strict`，核对 FR-MASTER-001、FR-MASTER-002、FR-MASTER-003、FR-MASTER-007、FR-USER-006、FR-USER-007 的需求追溯。
