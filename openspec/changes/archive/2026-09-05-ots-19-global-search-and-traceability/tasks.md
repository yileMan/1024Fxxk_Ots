## 1. 失败测试与验收基线

- [x] 1.1 为漏洞目录增加后端失败测试，覆盖产品版本筛选、产品/版本不匹配、排序白名单、稳定次序、分页边界和全部条件交集语义，并确认未新增 EOL/KEV 行为。
- [x] 1.2 为追溯接口增加后端失败测试，覆盖当前来源批次、候选分组、完整评估修订链、历史提交/审核事件、候选无评估和批次元数据不可用。
- [x] 1.3 增加授权与只读失败测试，覆盖管理员全局查询、普通用户混合范围裁剪、完全越权 `403`、未登录 `401`、非法参数 `422`、数量侧信道及查询不写审计。
- [x] 1.4 为目录 URL 状态和追溯页面增加前端失败测试，覆盖筛选/排序/分页恢复、前进后退、授权失效、分层空状态、错误反馈和不执行标记文本。
- [x] 1.5 使用系统 Chrome 增加失败的纵向 E2E 场景“组合查询 → 分享/刷新 → CVE 追溯 → 返回原查询”，保留 RED 测试结果作为实现基线。

## 2. OpenAPI 契约与查询模型

- [x] 2.1 扩展漏洞目录请求契约，增加 `product_version_id`、固定 `sort_by`/`sort_order` 和可恢复 `page_size`，为非法枚举、时间区间及产品版本组合返回稳定字段级错误。
- [x] 2.2 定义窄化追溯响应 schema，包含漏洞根、当前来源实际引用批次、候选、产品评估链及修订事件，不暴露完整包 JSON、绝对路径、长篇产品结论或内部证据。
- [x] 2.3 新增 `GET /api/v1/vulnerabilities/{vulnerability_id}/traceability` 路由契约并复用当前身份与错误模型，确认纯查询不接入审计写入。
- [x] 2.4 生成 OpenAPI 和 TypeScript 类型，连续生成两次并比较结果，确保 schema、枚举和前端类型稳定且无手写漂移。

## 3. 数据查询、授权与后端服务

- [x] 3.1 扩展漏洞目录 repository，以相关 `EXISTS` 实现产品版本交集条件和产品/版本从属校验，统一应用有效版本范围并在裁剪后计算总数及候选/任务数量。
- [x] 3.2 实现排序白名单、显式 NULL 行为和漏洞 ID 稳定次序，保证相同条件重复翻页不重排、不重复且不漏项。
- [x] 3.3 实现追溯的批量 repository 查询：一次读取漏洞与实际来源批次、一批读取可见候选、一批读取授权产品的全部评估修订，禁止候选或评估级 N+1 查询。
- [x] 3.4 在 service 中按 OTS 和产品 OTS 组装稳定追溯树，保留修订父子关系及修订自身的提交/审核事件，并固定显示“候选不等于产品受影响”。
- [x] 3.5 复用有效产品版本范围实现追溯节点裁剪，验证停用范围、混合授权、历史修订和直接 ID 访问均不泄露范围外产品、人员、状态、节点或计数。
- [x] 3.6 使第 1.1～1.3 节后端测试转绿，并运行相关 pytest 与覆盖率检查，确保新增后端代码覆盖率不低于 80%。

## 4. 前端组合查询与追溯页面

- [x] 4.1 扩展漏洞目录 API client，完全使用生成类型传递产品版本、排序、筛选和分页参数，并映射 `401/403/404/422` 与服务错误。
- [x] 4.2 将漏洞目录改为 Vue Router query 驱动，恢复全部有效条件，支持分享、刷新、前进/后退和从详情返回，清除空参数且不短暂展示默认或旧缓存结果。
- [x] 4.3 增加产品/产品版本联动条件、白名单排序和每页数量控件，产品与版本选项按当前用户有效范围提供，保留既有 KEV 兼容界面但不新增相关逻辑或验收。
- [x] 4.4 新增 CVE 追溯 API client、路由和页面，按来源与批次、候选 OTS、产品评估链、修订与审核事件分层展示，并提供加载、空组、无权、未找到、参数错误和重试状态。
- [x] 4.5 确保来源文本、匹配依据、变化摘要和修订原因只以文本呈现，不使用 `v-html`，且所有追溯区域保持只读、只链接到已有授权详情。
- [x] 4.6 使第 1.4 节组件测试转绿，并运行前端 Vitest/coverage、`npm run typecheck` 和生产构建，确保新增前端代码覆盖率不低于 80%。

## 5. MySQL 集成、索引与性能验证

- [x] 5.1 使用真实 MySQL 8 建立代表性产品、OTS、漏洞、候选、混合授权和长修订链数据，验证目录总数、稳定分页、追溯分组和历史事件与数据库事实一致。
- [x] 5.2 使用双会话在自动复评或审核创建新修订期间读取追溯，确认单次请求不返回断裂父链、多个当前标记或跨时点拼接结果，且读取不阻塞正常写入。
- [x] 5.3 对默认目录、产品版本+状态、OTS+状态、严重度+时间和追溯修订 SQL 执行 `EXPLAIN`/`EXPLAIN ANALYZE`，记录扫描行数、索引和耗时；只有证据表明必要时才增加最小复合索引。
- [x] 5.4 如需索引，新增一个编号化 SQL 迁移和回滚说明，验证空库安装、现有库升级、回滚及基础表仍为 11 张；如无需迁移，记录现有索引足够的执行计划证据。
- [x] 5.5 在正常规模和 10～20 个并发用户下运行组合查询及追溯性能测试，验证 95% 请求小于 3 秒，并回归候选生成、自动复评和评估写入性能。

## 6. 纵向回归、文档与交付检查

- [x] 6.1 使用系统 Chrome 使第 1.5 节纵向场景转绿，并覆盖混合授权裁剪、空候选/空评估链、长修订链、恶意标记文本和查询失败重试；不得下载 Playwright Chromium。
- [x] 6.2 回归 OTS-11 漏洞目录/详情、OTS-15 历史与比较、OTS-16 自动复评和 OTS-17 跨产品参考，确认查询不改变状态、行版本、修订数、待办或审计。
- [x] 6.3 运行完整后端 pytest/coverage、前端 Vitest/coverage、类型检查、生产构建、OpenAPI 漂移检查及系统 Chrome Playwright 全量回归，保留不能执行项目的实际环境错误。
- [x] 6.4 更新 `doc/Task.md`：记录 OTS-18 因当前无 EOL 业务暂缓，移除 OTS-19 对 OTS-18 的实施依赖，明确本 change 不新增 EOL/KEV 工作，并修正 ADR-03、风险表及跨产品参考的 OTS 编号错位。
- [x] 6.5 按 FR-WORK-003 的本次非 EOL/KEV 范围、FR-WORK-004、NFR 12.2、产品范围授权和 11 表数据基线完成双向需求追溯，明确 OTS-18、OTS-20、OTS-21 的剩余边界。
- [x] 6.6 运行 `openspec validate ots-19-global-search-and-traceability --strict --no-interactive` 及 OpenSpec 全量严格校验，确保 proposal、spec、design、tasks 一致且无错误。
- [x] 6.7 更新 OTS-19 完成证据，记录实现提交、测试数量、覆盖率、MySQL 执行计划与性能结果、迁移结论及剩余风险；仅在全部验收通过后同步主规格、归档 change 并提交到当前主干，不执行 `git push`。
