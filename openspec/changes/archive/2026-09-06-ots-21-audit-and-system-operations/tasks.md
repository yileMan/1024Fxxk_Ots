## 1. 失败测试与审计覆盖基线

- [x] 1.1 盘点 10 张非审计基础表的全部 API 与后台写入口，形成对象、动作、操作者、事务、幂等和详情字段矩阵，并用测试证明现有覆盖缺口。
- [x] 1.2 为统一审计契约增加后端失败测试，覆盖成功 `insert/update/delete/batch_upsert`、业务与审计同事务、提交异常、校验失败、乐观锁冲突、幂等无变化及系统任务空操作者。
- [x] 1.3 为审计详情增加失败测试，覆盖新增关键值、更新 `from/to`、删除原因、批次汇总、schema 版本、字段白名单、长度边界，以及密码、Cookie、长文本、审核意见和导入正文脱敏。
- [x] 1.4 为非数据库变更活动增加回归测试，证明登录、纯查询、审计查看、评估导出、备份状态读取和基础/详细健康探测在成功、失败与拒绝时均不写 `audit_log`。
- [x] 1.5 为审计查询和运行信息页面增加前端组件失败测试及系统 Chrome 纵向 RED 场景，覆盖权限、组合筛选、分页、差异安全展示、局部故障、未知/陈旧状态和重试。

## 2. OpenAPI 与权限契约

- [x] 2.1 定义审计列表、详情、筛选、游标分页、总数和错误响应 schema，固定动作/对象白名单、带时区闭区间及 `401/403/404/422` 语义。
- [x] 2.2 新增管理员只读 `GET /api/v1/audit-logs` 与 `GET /api/v1/audit-logs/{audit_log_id}` 契约，不提供审计修改、删除、清理或导出端点。
- [x] 2.3 定义运行信息整体与六个分项 schema，固定 `ok/warning/error/unknown` 严重度、观测时间、安全摘要和局部失败返回规则。
- [x] 2.4 新增管理员只读 `GET /api/v1/system/operations` 契约，并保持 `GET /api/v1/health` 的公开最小响应不暴露内部详情。
- [x] 2.5 生成 OpenAPI 与前端 TypeScript 类型，连续生成两次并比较结果，确保契约和类型无手写漂移。

## 3. 统一审计写入与数据查询

- [x] 3.1 实现不自行提交的统一审计构造器，限制四种动作并固定单条变化、增删值、批次汇总、schema 版本、值归一化、白名单和脱敏规则。
- [x] 3.2 按第 1.1 节矩阵逐个迁移用户、授权、产品、版本、OTS、产品 OTS、导入、匹配、任务生成、评估编辑和修订流程的审计调用，保持各业务事务边界不变。
- [x] 3.3 修复矩阵发现的遗漏或错误审计，确保一个实际变化可追溯、无变化不记录、导入按批次聚合，并且不会递归审计 `audit_log`、迁移元数据、日志或文件状态。
- [x] 3.4 实现管理员审计 repository/service，按全部筛选条件求交集并使用 `(created_at, id)` 降序 keyset 分页和同条件计数；列表只返回摘要，详情兼容新旧 JSON 形状。
- [x] 3.5 对全部审计读端点强制管理员权限和只读 session，验证非管理员统一 `403` 不产生存在性侧信道、未登录 `401`、不存在 `404`、非法筛选 `422`。
- [x] 3.6 使第 1.2～1.4 节后端审计测试转绿，运行相关 pytest 与覆盖率检查，确保新增后端代码覆盖率不低于 80%。

## 4. 系统运行信息后端

- [x] 4.1 实现应用版本探测，从构建/部署元数据读取版本和可选提交标识；缺失时返回 `unknown`，不得运行 Git 命令或暴露主机信息。
- [x] 4.2 实现有短超时的数据库轻量只读探测，只返回可用性和耗时，不返回 DSN、SQL 错误、数据库横幅或堆栈。
- [x] 4.3 实现配置持久化路径所在文件系统的容量探测和阈值状态，只读取总量与剩余空间，不递归扫描或访问未配置路径。
- [x] 4.4 实现 `import_batch` 最新批次与最新失败批次查询，返回批次号、状态、时间和受控计数，排除绝对路径、manifest 和错误正文。
- [x] 4.5 实现 `backup-status-v1.json` 只读解析与校验，覆盖未配置、不存在、成功、失败、陈旧、大小超限、非法 JSON、未知版本、符号链接/路径逃逸和原子替换期间读取。
- [x] 4.6 实现六分项独立捕获异常与确定性整体严重度聚合；单项失败仍返回其他状态，认证阶段数据库不可用时保持统一服务错误且不绕过权限。
- [x] 4.7 增加结构化运行日志的非敏感事件与耗时字段，验证日志和全部状态探测均不进入 `audit_log`，也不触发备份或互联网访问。
- [x] 4.8 使运行信息后端测试转绿，运行相关 pytest、超时/资源释放和覆盖率检查，确保新增后端代码覆盖率不低于 80%。

## 5. 系统管理前端

- [x] 5.1 新增“系统管理－变更记录”路由和管理员导航入口，实现对象、用户、动作和带时区时间区间筛选，筛选变化后重置游标。
- [x] 5.2 实现审计稳定分页、总数、空状态、加载和错误重试，并在只读抽屉/对话框中安全展示操作者、对象、动作和新旧格式关键差异。
- [x] 5.3 对未知 JSON 键、空操作者、长值和恶意 HTML/脚本使用纯文本及受控折叠展示，不提供修改、删除、清理或导出操作。
- [x] 5.4 新增“系统管理－运行状态”路由和管理员导航入口，按整体与六分项展示状态、观测时间、容量、批次和安全摘要。
- [x] 5.5 实现主动刷新、加载、局部故障、未配置/无记录、陈旧数据和整页失败反馈；刷新失败时不得把上次状态继续标为正常，不实现高频自动轮询。
- [x] 5.6 使第 1.5 节组件测试转绿，运行 Vitest/coverage、`npm run typecheck` 和生产构建，确保新增前端代码覆盖率不低于 80%。

## 6. MySQL、性能与纵向集成验证

- [x] 6.1 在真实 MySQL 8 验证业务数据与审计记录共同提交或共同回滚，覆盖用户写入、授权、产品/OTS 管理、导入批次、任务生成及评估提交/审核/退回/修订。
- [x] 6.2 生成包含相同毫秒时间、空操作者、四种动作、多个对象及新旧详情格式的代表性十年审计数据，验证组合筛选、总数和 keyset 翻页无重复漏项。
- [x] 6.3 对无筛选、用户+时间、对象+时间和动作+时间查询执行 `EXPLAIN`/`EXPLAIN ANALYZE` 并记录扫描量与耗时；只有证据证明必要时才增加最小编号化索引迁移和回滚说明。
- [x] 6.4 在正常规模和 10～20 并发用户下验证审计列表、详情和运行状态 P95 小于 3 秒，并验证探测超时不会耗尽数据库连接或请求线程。
- [x] 6.5 使用系统 Chrome 使“管理员筛选变更记录 → 翻页 → 查看差异”和“查看正常状态 → 注入局部故障 → 刷新恢复”纵向场景转绿，并覆盖普通用户和未登录拒绝；不得下载 Playwright Chromium。

## 7. 回归、文档与交付检查

- [x] 7.1 回归 OTS-02 用户/授权、OTS-10 任务生成、OTS-15 修订重提及所有后续写流程，确认统一审计未改变状态机、权限、乐观锁、批量语义或错误码。
- [x] 7.2 运行完整后端 pytest/coverage、前端 Vitest/coverage、类型检查、生产构建、OpenAPI 漂移检查及系统 Chrome Playwright 回归，并保留任何环境阻塞的实际错误。
- [x] 7.3 验证数据库仍为 11 张应用基础表，未新增运维/告警/审计任务表、队列、监控平台或互联网依赖；若新增索引迁移，验证升级与回滚且不修改历史审计。
- [x] 7.4 文档化统一审计详情格式、写路径矩阵、管理员 API、状态分项、阈值配置和 `backup-status-v1.json` 生产者契约，为 OTS-22 备份脚本实现提供输入。
- [x] 7.5 更新 `doc/Task.md` 的 OTS-21 状态、范围、完成证据和需求追溯，按 FR-AUDIT-001、FR-AUDIT-002、FR-AUDIT-003、系统方案 11.3 及关键验收规则 19 建立双向证据。
- [x] 7.6 运行 `openspec validate ots-21-audit-and-system-operations --strict --no-interactive` 及 OpenSpec 全量严格校验，确保 proposal、spec、design、tasks 一致且无错误。
- [x] 7.7 仅在全部验收通过后同步主规格、归档 change 并提交到当前主干，使用中文提交说明且不执行 `git push`。
