## 1. 测试基线与契约

- [ ] 1.1 为 `user_product_scope` 空库迁移、从 OTS-04 升级、约束/索引和受控回滚先编写失败测试，并确认应用基础表总数为 11
- [ ] 1.2 为产品级展开、版本级隔离、范围并集、管理员全局访问及停用产品/版本失效先编写服务层和 MySQL 集成失败测试
- [ ] 1.3 为授权查询、授予、重复幂等、撤销、非法类型、跨产品版本、非管理员访问和事务审计回滚先编写 API 失败测试
- [ ] 1.4 为产品/版本列表与详情裁剪、分页总数、授权版本 OTS 清单读取、范围外直接 ID 访问和普通用户写入拒绝先编写路由失败测试
- [ ] 1.5 为授权配置组件、当前范围状态、加载/空/错误反馈、操作可见性和 403 路由先编写 Vitest/组件失败测试
- [ ] 1.6 固定 OpenAPI 请求/响应、HTTP 状态和稳定错误码 `PRODUCT_SCOPE_FORBIDDEN`，覆盖角色、范围、资源分配和禁止自审的组合边界

## 2. 数据迁移与模型

- [ ] 2.1 新增 `008_user_product_scope.sql`，按数据基线创建字段、`scope_type`/空值约束、外键、`uk_user_product_scope` 和两个范围查询索引
- [ ] 2.2 新增 `008_user_product_scope.rollback.md`，写明应用回滚顺序、授权备份/导出前置条件、删表影响和禁止级联删除的验证步骤
- [ ] 2.3 新增 SQLAlchemy `UserProductScope` 模型及必要关系，确保 `scope_key` 只能由服务端按 `product:<id>` 或 `version:<id>` 生成
- [ ] 2.4 在真实 MySQL 上验证空库与升级库迁移、重复执行检测、非法组合拒绝、外键保护、索引和受控回滚

## 3. 后端授权核心

- [ ] 3.1 实现授权 Repository 的显式范围查询、精确增删、重复重读和基于 SQL 谓词的有效产品/版本范围计算
- [ ] 3.2 实现授权 Service 的目标存在性、版本归属、范围类型校验、产品级/版本级并集、停用实体过滤和管理员全局语义
- [ ] 3.3 实现授予/撤销与 `audit_log` 同事务提交，验证重复、校验失败、403、唯一键竞态和事务失败均不产生多余审计
- [ ] 3.4 实现认证、管理员、产品范围、版本范围及未来负责人/审核人条件可组合的依赖，统一返回 401、403 与 404
- [ ] 3.5 新增 `GET/POST /api/v1/users/{user_id}/scopes`、`DELETE /api/v1/users/{user_id}/scopes/{scope_id}` 和 `GET /api/v1/scopes/me` 路由及 Schema
- [ ] 3.6 将产品/版本 GET 列表和详情接入 SQL 层范围裁剪，确保 `items` 与 `total` 使用相同范围谓词，写接口继续仅管理员可用
- [ ] 3.7 将产品版本 OTS 清单 GET 接入版本范围校验，保持 OTS 主数据、关联增删和 CSV 管理接口仅管理员可用

## 4. 前端授权体验

- [ ] 4.1 更新 OpenAPI 文档并重新生成 `front/src/api/generated.ts`，基于生成类型实现 scopes API client，不手写重复模型
- [ ] 4.2 在用户管理中增加授权配置入口和组件，支持按产品分组查询、授予、幂等反馈和撤销产品级/版本级显式范围
- [ ] 4.3 在授权配置中显示产品级覆盖、重叠版本级授权、停用范围无效标识，以及加载、空状态、字段错误、403 和服务失败
- [ ] 4.4 在前端内存状态中加载当前用户范围摘要，组合固定角色与范围控制导航/操作入口且不写入浏览器持久存储
- [ ] 4.5 统一处理直接路由越权和 API 403 到明确的 `ForbiddenPage`/权限反馈，并保持 404 与空数据语义独立
- [ ] 4.6 保持现有产品管理、OTS 和用户与角色管理导航仅管理员可见，验证普通用户只读 API 范围不会暴露管理员写入口

## 5. 集成与纵向验收

- [ ] 5.1 完成“管理员给产品负责人授予产品级范围 → 用户读取全部有效版本和对应 OTS 清单 → 撤销后直接 API 返回 403”的纵向场景
- [ ] 5.2 完成“审核人仅获单版本范围 → 同产品其他版本列表不可见且详情/写入直接请求被拒绝”的纵向场景
- [ ] 5.3 验证管理员无显式范围仍全局读取，但不因管理员角色绕过未来负责人、审核人和禁止自审组合校验
- [ ] 5.4 验证停用产品/版本使范围失效但保留显式授权和审计，重复授权/撤销及并发唯一键竞态保持幂等
- [ ] 5.5 运行后端单元与 MySQL 集成测试、前端 Vitest/组件测试、Playwright 冒烟测试和前后端构建，并修复本 change 引入的失败
- [ ] 5.6 检查前后端新增代码覆盖率均不低于 80%，核心范围矩阵、事务回滚和直接 API 越权场景全覆盖

## 6. 文档与验收

- [ ] 6.1 更新开发文档，说明范围 API、`scope_key` 规则、共享授权依赖、403 错误、迁移与回滚操作，不记录真实凭据
- [ ] 6.2 对照 `FR-USER-003`、`FR-USER-004` 和权限规则 1～9 更新需求追溯与测试证据，并确认范围外功能未被引入
- [ ] 6.3 执行 `openspec validate ots-05-product-scope-authorization --strict --no-interactive` 并修复全部校验错误
- [ ] 6.4 复核数据库仍为 11 张应用基础表、OpenAPI 与 TypeScript 类型一致、关键授权写入同事务审计、失败回滚无审计残留
