## 1. 测试基线与契约

- [x] 1.1 先为 `submit` 编写后端失败测试，覆盖完整提交、全部缺失字段路径、`applicability=pending`、高风险处置依据、来源 v3.1 不可用可提交、环境结果失配、越权、自审分配、非当前/非 `pending` 和旧 `row_version`。
- [x] 1.2 先为 `approve` 与 `return` 编写后端失败测试，覆盖当前指定审核人、固定角色与产品范围、重新分配、禁止自审、状态/并发冲突、重复动作、退回意见必填/超限和额外结论字段拒绝。
- [x] 1.3 更新 OpenAPI 契约测试，固定三个 `POST` 动作接口、请求字段、详情动作能力及提交/审核留痕响应，并确认错误响应包含稳定 `403/409/422` 语义。
- [ ] 1.4 重新生成前端 TypeScript API 类型并执行漂移检查，确保不手写重复请求或响应模型。

## 2. 数据访问与后端实现

- [x] 2.1 扩展评估详情 schema 和查询，返回提交/审核留痕及服务端计算的 `can_submit/can_approve/can_return` 与不可用原因，同时保持范围外数据不泄露。
- [x] 2.2 抽取并复用草稿规范化与条件校验，增加基于持久化快照的提交完整性门和环境评分重算一致性检查；来源 v3.1 不可用时保持空分但允许其他内容完整的提交。
- [x] 2.3 在 repository 实现包含评估 ID、`is_current`、预期状态和 `row_version` 的条件状态更新，保证每次成功动作只递增一次行版本并能区分并发/状态冲突。
- [x] 2.4 实现提交服务与 `/submit` 路由，实时校验 `product_owner`、有效范围、当前负责人、当前审核人非本人和 `status=pending`，原子写入 `submitted_by/submitted_at`、状态和审计。
- [x] 2.5 实现审核授权公共校验及 `/approve` 路由，按产品版本当前指定审核人而非历史 `reviewer_id` 分配，原子写入 `completed/approved`、实际审核人、审核时间和审计。
- [x] 2.6 实现 `/return` 路由，规范化并校验必填意见，只写 `returned/returned` 与实际审核留痕和审计，不接受结论字段、不创建新修订。
- [x] 2.7 收紧 OTS-14 阶段草稿编辑状态，使 `submitted`、`returned`、`completed` 及历史修订保持只读，并增加现有 `returned` 保存回归测试。
- [x] 2.8 验证动作更新与 `audit_log` 同事务提交，覆盖审计写入失败整体回滚、无重复审计、敏感长文本不进入审计详情。

## 3. 前端交互

- [x] 3.1 为评估详情页增加提交/审核留痕和服务端动作能力适配，确保按钮由能力字段驱动且 `submitted/returned/completed` 结论只读。
- [x] 3.2 实现提交确认对话框、执行中防重复和成功后以服务端响应刷新状态、行版本及实际提交留痕。
- [x] 3.3 实现审核区、通过确认和退回意见对话框，提供意见必填/超限字段反馈，且审核人不能编辑负责人结论。
- [x] 3.4 实现需重新分配审核人、权限/分配变化、并发冲突和服务错误提示；失败时不本地猜测状态，保留退回意见并提供刷新或重试路径。
- [x] 3.5 明确展示 OTS-14 的退回修订只读提示和“等待创建新修订”边界，不提前实现 OTS-15 的修订或重新提交入口。
- [x] 3.6 增加前端组件测试，覆盖各角色/状态动作展示、三类确认交互、退回校验、请求防重、成功刷新和全部错误反馈。

## 4. 集成与端到端验证

- [ ] 4.1 使用 MySQL 集成测试验证 `pending → submitted → completed`、`submitted → returned`、并发通过/退回仅一个成功、历史提交/审核留痕不被重写和失败事务零审计。
- [ ] 4.2 使用系统 Chrome 运行 Playwright，覆盖负责人提交后二次确认与只读、审核人通过、审核人退回必填意见、自审提交提示重新分配，以及直接调用越权接口被服务端拒绝。
- [ ] 4.3 回归工作台 `submitted` 待审核分配、评估详情草稿保存、CVSS v3.1 环境预览/保存和产品范围授权，确认未引入新表、通知或跨产品参考行为。

## 5. 质量、文档与验收

- [ ] 5.1 运行后端 pytest（含 MySQL 专项）及覆盖率检查，确保新增后端代码覆盖率不低于 80%，并记录测试数量和关键模块覆盖率。
- [ ] 5.2 运行前端 Vitest、类型检查、生产构建及覆盖率检查，确保新增前端代码覆盖率不低于 80%；E2E 优先使用系统 Chrome，不下载 Playwright Chromium。
- [ ] 5.3 连续生成两次 OpenAPI 并比较结果，确认契约与生成类型稳定且无漂移。
- [ ] 5.4 运行 `openspec validate ots-14-assessment-submit-and-review --strict` 及项目全量 OpenSpec 严格校验。
- [ ] 5.5 按 FR-REVIEW-002/003/005/006、FR-USER-005、FR-ASSESS-009/010 和 FR-REVIEW-001 分阶段边界完成需求追溯，明确跨产品参考仍属 OTS-17、退回新修订与重新提交仍属 OTS-15。
- [ ] 5.6 更新相关开发文档和 `doc/Task.md` 完成证据，记录实现提交、测试/构建结果、覆盖率、迁移结论及剩余风险；仅在全部验收通过后归档 change。
