## 1. 前置条件与失败测试

- [ ] 1.1 确认 `ots-00-platform-foundation` 已完成并归档，记录可用的 MySQL 迁移、后端分层、统一错误、Vue TypeScript、API 类型生成和测试命令；前置条件不满足时停止实施
- [ ] 1.2 根据 `local-authentication` Spec 先添加后端失败测试，覆盖管理员初始化、密码摘要、登录成功/失败、Cookie 篡改/过期、当前用户、停用实时失效、幂等退出、Origin 拒绝和认证行为无审计记录
- [ ] 1.3 先添加前端失败测试，覆盖登录校验、身份恢复、路由回跳、会话失效/停用反馈、退出以及不使用浏览器存储保存身份
- [ ] 1.4 先添加 Playwright 失败场景，贯通初始化管理员、登录、刷新恢复身份和退出后的受保护路由拒绝

## 2. 契约与安全配置

- [ ] 2.1 定义登录请求、公开用户响应和 `/api/v1/auth/login`、`/logout`、`/me` 的 OpenAPI 契约，固定 HTTP 状态及 `AUTH_INVALID_CREDENTIALS`、`AUTH_SESSION_INVALID`、`AUTH_USER_DISABLED`、`AUTH_ORIGIN_REJECTED` 错误码
- [ ] 2.2 增加 Cookie 名称、两小时固定时效、签名密钥、管理平台允许来源、生产 `Secure` 开关和 Argon2id 参数配置，并验证生产环境缺失或弱密钥时拒绝启动
- [ ] 2.3 从 OpenAPI 重新生成前端 TypeScript API 类型并加入契约漂移检查，不手写重复的认证模型

## 3. 数据迁移与初始化管理员

- [ ] 3.1 编写连续编号的 MySQL 迁移，按数据基线创建 `app_user` 和 `audit_log` 的字段、约束、索引及外键，并确认应用基础表总数不超过 11
- [ ] 3.2 编写对应回滚说明和受控回滚脚本，验证仅在无下游引用的首次上线失败场景按 `audit_log`、`app_user` 顺序回退
- [ ] 3.3 实现 `app_user` 模型与 Repository，正确读取 JSON 固定角色、用户状态和 `last_login_at`
- [ ] 3.4 实现 Argon2id 密码摘要/校验服务和 12～256 字符策略，确保日志、异常及数据库均不暴露明文密码
- [ ] 3.5 实现幂等初始化管理员命令，通过无回显交互或专用环境变量接收密码，验证重复执行不覆盖现有用户
- [ ] 3.6 在空库和升级测试库执行迁移、约束检查、初始化和回滚验证，保留结果证据

## 4. 后端认证

- [ ] 4.1 实现版本化最小 Cookie 载荷的签发、签名校验、固定到期和匹配属性清除，并覆盖开发/生产 Cookie 属性
- [ ] 4.2 实现当前用户认证依赖，每次请求按用户 ID 重新查询状态和角色，正确处理缺失、篡改、过期、不存在及停用用户
- [ ] 4.3 实现登录 Service 和 `POST /api/v1/auth/login`，统一未知账号、错误密码和停用账号响应，成功后更新 `last_login_at` 且不写 `audit_log`
- [ ] 4.4 实现幂等 `POST /api/v1/auth/logout` 和 `GET /api/v1/auth/me`，只返回公开用户字段并在失效场景清除 Cookie
- [ ] 4.5 实现严格 scheme/host/port 的 Origin 校验，在认证 Cookie 写请求及登录/退出业务执行前拒绝缺少或不匹配来源
- [ ] 4.6 检查认证应用日志脱敏，确认不记录密码、密码摘要、完整 Cookie、签名密钥或完整登录请求体

## 5. 前端认证体验

- [ ] 5.1 实现登录页及表单状态，支持提交中、字段错误、统一凭据错误和服务失败反馈
- [ ] 5.2 实现内存认证状态和单次 `/auth/me` 初始化，确保不向 `localStorage` 或 `sessionStorage` 写入身份、角色或认证令牌
- [ ] 5.3 实现受保护路由守卫和安全站内回跳，拒绝把外部 URL 或协议作为登录后目标
- [ ] 5.4 在基础布局展示当前用户信息和退出入口，退出成功或幂等响应后清空内存身份并返回登录页
- [ ] 5.5 接入全局认证错误处理，区分会话失效与账号停用提示，并避免重复跳转或并发身份恢复请求

## 6. 集成与验收

- [ ] 6.1 运行后端单元和 MySQL 集成测试，确认所有 Spec 场景通过且后端新增代码覆盖率不低于 80%
- [ ] 6.2 运行前端组件测试和生产构建，确认前端新增代码覆盖率不低于 80% 且生成 API 类型与 OpenAPI 一致
- [ ] 6.3 运行 Playwright 登录纵向场景，验证初始管理员登录、刷新恢复、错误密码、停用即时失效、Cookie 属性、会话失效反馈和退出
- [ ] 6.4 验证跨来源写请求没有业务写入，完整认证流程没有新增 `audit_log`，并检查日志中不存在认证秘密
- [ ] 6.5 执行 `openspec validate ots-01-local-authentication --strict --no-interactive`，修复全部校验问题

## 7. 文档与追溯

- [ ] 7.1 更新 `.env.example` 和本地/部署说明，记录签名密钥生成、允许来源、Cookie Secure、初始化管理员和密钥轮换导致全员退出的行为，不提交真实凭据
- [ ] 7.2 复核 FR-USER-001、FR-USER-002、FR-USER-004 为部分支撑、NFR 12.3 为本变更直接落实，并明确完整用户管理和授权继续由 OTS-02、OTS-05 验收
- [ ] 7.3 汇总严格校验、迁移、后端测试、前端测试/构建、覆盖率和 Playwright 证据；全部通过后再归档 change 并更新 `doc/Task.md` 状态及完成提交号
